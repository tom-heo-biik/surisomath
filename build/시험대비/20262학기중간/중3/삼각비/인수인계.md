# 인수인계 — 2026. 9. 23. 시험대비 삼각비 초안 완성 + 삼각비·원과직선·이차함수 독립 검토 반영

## 2026. 9. 24. 폴더 이름
- 규칙이 서기 전에 만든 첫 삼각비 학습지(열 문제)는 지우고, 삼각비2를 삼각비로 이름을 바꿨다(사진·PDF 이름도). 이력은 git에 있다.

## 독립 검토(서브에이전트 셋, 사진↔yaml↔크롭 대조 + 70문제 독립 풀이) 결과와 반영
- 정답 70/70 일치, 수치·선지 순서 불일치 0.
- 삼각비: 023 BF→OF(책은 반지름 OF, 내 오독), 025 OA 점선 직선 + 6cm, 011 36° 밖으로, 026 6m을 9시 살 위에(살을 끊고),
  004 −1 지시선, 030 D 이름 아래로·E 이름 뺌(지문이 안 부름), 002 "점 C가 점 P에" 지문에, 027 변 EF 긋기(선생님 지적 —
  색칠 경계가 비어 있었다), 011·012·025·028 문장 안 ∠ → "각".
- 반지름 규칙 정정: "직선으로"는 반지름 선분을 그으라는 뜻이었다. 선분 옆에 글만 두었더니 020의 13cm·8cm가 이상하다고
  해서 책 방식(선분 + 점선 호 + 글)으로 되돌렸다 — 삼각비 007 r·r', 원과직선 014 4·4, 020 13cm(OO' 위 낮은 호, 글은 O' 쪽)·
  8cm(O'T' 오른쪽 호)·3cm(OT 왼쪽 호 + 지시선).
- 원과직선: 020 13cm를 OO' 위 O' 쪽으로(선생님 지적),
  018 같은 각 표시 점으로. 004 직각 표시(OM = 1이라 O에 붙음)·008/014/015 귀퉁이 직각 표시·002 16 곡선은 책과 같아 둠.
- 이차함수: 008 직선 이름 내림, 019 눈금 2·3·4 오른쪽으로, 020 Q 살짝 오른쪽, 012 ㄷ "실수 a" → "a"(원문).
- 공통(스킬): rich()가 명사+이름·"(단,"을 한 어절로 묶음. grind_figure.save()에 색칠 경계 검사와 점 이름 주석,
  build.py에 지문↔그림 점 이름 대조. angle(ticks=1)은 점, ticks=2는 ×. 견본·연마 9.12·수행평가 회귀 경고 0(HTML의 span
  묶음과 SVG 끝 주석이 바뀌어 git diff는 난다 — 의도한 변화). 낡은 삼각비(1) 폴더는 git checkout으로 되돌려 두었다.


새 세션은 이 파일과 `.claude/skills/surisomath-exam/SKILL.md`만 읽으면 이어 갈 수 있다.
오늘 배운 규칙은 SKILL.md에 넣었다(삼각비의 표·소문항, 삽화 문제는 벡터 선화로 고증, 그릴 수 없는 각은 생략 표시,
반지름은 선분 + 이름, 정사각뿔 투영).


## 지금 상태
- 삼각비 학습지 초안 완성: build/시험대비/20262학기중간/중3/삼각비/. 서른 문제, 30쪽(학생 15 + 선생님 15),
  build.py 경고 0개, 정답 서른 개 sympy 재검산 끝, 그림 스물아홉 장 크롭 눈 검토 끝.
- 사진은 photos.py로 수리소_시험대비_2026_2학기중간_중3_삼각비_001~030.jpg로 바꿨다(git mv, 미커밋).
- 스킬 파일을 고쳤다: build.py에 table(삼각비의 표)·subs(소문항) 추가, exam.css에 table.stat·ol.sub. 견본 회귀 빌드
  종료 코드 0, git 변경 없음. 원과직선·이차함수·삼각비도 --no-figures로 다시 돌려 확인했다(아래 '회귀' 줄).
- 커밋·푸시는 아직 안 했다(시킬 때만).


## 선생님이 정한 것 (오늘, 작업 중에)
- 009 도로 표지판·026 관람차: 인터넷 이미지나 사진이 아니라 벡터 선화로, 대신 책 그림의 구성을 다 옮긴다
  (표지판은 116번 도안, 관람차는 살·곤돌라·A자 기둥·지면 띠). 처음 그림(원 하나에 막대 하나)은 "형편없다".
- 001 지구·달·태양: 88°를 속여 그리지 않는다. 시선을 끊고(생략 표시는 "…", "//"가 아니라) 태양 쪽은 실제 2°로.
- 007 반지름: 곡선 치수가 아니라 반지름 선분을 긋고 옆에 r, r'. 026의 6m도 살 옆에 이름으로.
- 013 정사각뿔: D가 모서리 VA에 붙은 첫 그림은 어색. 밑면을 돌려(195°·285°·15°·105°) D를 안쪽에.
- 027 평행사변형: 밑변 BC가 가로가 되게 돌렸다(첫 그림은 기울어 있었다).


## 선생님이 볼 것 — 내가 판단한 것
- 지문은 원과직선처럼 평가원 문형으로 퇴고했다. 바꾼 것·채운 것은 problems.md 머리에 다 적었다. 특히
  001 두 각(88°, 90°)을 지문에, 003 "점 E는 선분 AB의 연장선 위", 013 "m ≤ cos x ≤ M" → "최댓값 M, 최솟값 m",
  025 "(단, 점 A는 각 XOY의 내부에 있다.)".
- 삼각비의 표(001·009·010)는 수행평가 통계 표와 같은 격자로 조판했다. 교과서의 삼각비의 표가 격자라서다.
- 016·018 선지는 너비가 76.3pt(3열 한계)를 2.5pt 넘어 규칙대로 1열 다섯 줄이다.
- 001 그림의 위쪽 점선 "…"은 지구 시선이 끊긴 자리에서 태양 쪽 쐐기로 내려가며 찍혀 비스듬하다. 생략의 뜻은 통하지만
  보기 싫으면 점 셋을 가로로만 찍는 방법이 있다.
- 002 접힌 부분은 색칠하지 않았다(문제가 색칠을 가리키지 않는다). 책은 접힌 부분을 노랗게 칠했다.
- 030 겹치는 부분(사각형 AB'ED), 027 사각형 EFHG, 029 사각형 ABCD만 10% 틴트다.


## 회귀
- 견본(templates/sample): 경고 0, git 변경 없음.
- 원과직선·이차함수·삼각비 --no-figures 재빌드: 셋 다 그리드 이상 없음, 경고 0, git 변경 없음.
- 013 정사각뿔: 겨냥도로 그렸다가 선생님 지시("투시도로 ㄱㄱ")로 핀홀 투시도로 바꿨다(figures.py의 perspective, EYE·LOOK·FOCAL).
- 009 도로 표지판: 선화로 그렸다가 선생님 지시("009 그림도 해결 ㄱㄱ", 처음 제안이 인터넷 자료)로 위키미디어 공용의
  KR_road_sign_116.svg(정부 저작물, 퍼블릭 도메인)를 assets/에 받아 300dpi 래스터로 심었다. 학습지에서 유일한 색 그림이다.
  흑백으로 바꾸려면 figures.py p9에서 img를 회색조로 변환하면 된다.


## 주의
- problems.md는 선생님이 직접 고친다. 다시 Read한 뒤에만 Write한다.
- TeX가 든 파일(yaml·figures.py)은 Edit/Write로. Bash heredoc은 역슬래시를 한 겹 벗긴다.
- yaml 흐름 목록 `[…]` 안의 `'$2^{\circ}$'`는 작은따옴표 필수(중괄호를 YAML이 매핑으로 읽는다).
- 콘솔이 cp949라 sympy 결과에 √·π를 print하면 죽는다. PYTHONIOENCODING=utf-8로 돌린다.
