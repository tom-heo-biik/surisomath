# 수리소 시험대비 학습지 작업 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 시험대비 학습지 양식을 스킬 surisomath-exam으로 굳히고, 첫 단원 build/시험대비/20262학기중간/중3/삼각비(문제 열 개, 그림 아홉)를 학생 쪽 다섯 장 + 선생님 쪽 다섯 장 PDF로 뽑는다.

**Architecture:** surisomath-a4(판형·그리드·서체·render.py) 위에 exam.css를 얹고, 연마(surisomath-grind)의 grind_figure.py(그림)와 build.py의 rich()(본문 수식 조판)를 모듈로 빌려 쓴다. 새 build.py가 problems.yaml 하나로 그림 → 수식 SVG → HTML → PDF → 검사를 한 번에 한다. 한 쪽에 문제 둘(두 단), 문제 아래는 빈 풀 자리, 선생님 쪽은 같은 짝에 정답 줄과 풀이.

**Tech Stack:** Python 3.13, WeasyPrint 69(render.py), matplotlib 3.10(mathtext·SVG), pdfplumber, pymupdf(눈으로 볼 PNG), fontTools(선지 너비), PyYAML

설계: docs/superpowers/specs/2026-09-14-시험대비-design.md

---

## 파일 구조

| 파일 | 하는 일 |
|---|---|
| `.claude/skills/surisomath-a4/templates/render.py` (수정) | `--boxes`에 img뿐 아니라 class에 `box`가 든 요소의 상자도 적는다. 풀 자리(div.work.box)의 위 끝을 재려는 것 |
| `.claude/skills/surisomath-grind/templates/grind_figure.py` (수정) | 도우미 다섯: `angle`(각 표시), `tick`(같은 길이 표시), `seg`·`poly`의 `dashed`(점선 도형), `axis`(회전축 일점쇄선), `name`(점 이름·변의 길이 글) |
| `.claude/skills/surisomath-grind/SKILL.md` (수정) | 위 도우미를 그림 절에 적는다 |
| `.claude/skills/surisomath-exam/SKILL.md` (신규) | 시험대비 양식 규격과 작업 순서 |
| `.claude/skills/surisomath-exam/templates/exam.css` (신규) | base.css 위에 얹는 시험대비 스타일. 두 단·세로선·머리줄·이름 칸·선지 열·정답 줄 |
| `.claude/skills/surisomath-exam/templates/build.py` (신규) | problems.yaml → 그림 → 수식 → HTML → PDF → 검사 |
| `.claude/skills/surisomath-exam/templates/sample/` (신규) | 견본 두 문제(객관식 5열 하나, 그림 있는 서술형 하나). 새 단원은 여기서 복사해 출발한다 |
| `README.md` (수정) | surisomath-exam 절 |
| `build/시험대비/20262학기중간/중3/삼각비/problems.yaml` (신규) | 열 문제의 조판본·선지·정답·풀이 |
| `build/시험대비/20262학기중간/중3/삼각비/figures.py` (신규) | 그림 아홉 |
| `build/시험대비/20262학기중간/중3/삼각비/problems.md` (기존, 미추적) | 선생님 원문. 손대지 않고 git에 올린다 |

정본 좌표 몇 가지(설계 문서와 같다): 본문 영역 475×682pt, 머리줄 11~33pt, 번호 44~66pt, 지문 77pt부터, 가운데 세로선 x=237pt(쪽 기준 297pt) 44~682pt, 단 글 너비 229/230pt, 풀 자리 바닥 754pt(쪽 기준).

시험대비 표기 규칙(problems.yaml에 지문을 옮길 때): 선분 AB → `$\overline{\mathrm{AB}}$`, 각 → `$\angle\mathrm{BAC}$`, 삼각형 → `$\triangle\!\mathrm{ABC}$`(`\!` 없으면 벌어진다), 사각형 → `$□\mathrm{ABCD}$`(유니코드 □. `\square`는 mathtext에 없다), 각도 → `$35^{\circ}$`(중괄호 필수. `^\circ`는 벌어지고 수식 안 °는 γ로 찍힌다), 루트 → `\sqrt{}`, sin·cos·tan → `\sin x`. 직각삼각형·정사각형·직사각형·점·지점·직선·중점은 낱말 그대로, 이름만 `$\mathrm{ABCD}$`. 치수(80m, 4cm)는 글자. 직선 이름 `$l$`과 변수 `$x$`는 이탤릭.

---

### Task 1: render.py --boxes에 class box 요소를 더한다

**Files:**
- Modify: `.claude/skills/surisomath-a4/templates/render.py:252-270` (image_boxes)

- [ ] **Step 1: image_boxes를 고친다**

`image_boxes` 함수를 이렇게 바꾼다(img 조건 앞에 class 검사가 들어간다).

```python
def image_boxes(doc) -> list:
    """쪽마다 img 요소와 class에 box가 든 요소의 내용 상자(pt, 쪽 왼쪽 위 기준)와 class를 모은다.

    글줄에 끼워 넣은 이미지(본문 안 수식)가 서로 겹치는지는 PDF만 봐서는 알기
    어렵다. 레이아웃 결과에서 바로 뽑아 부르는 쪽(--boxes)이 살피게 한다. class에
    box를 단 블록 요소의 위 끝을 알면 그 아래 남은 자리를 잴 수 있다(시험대비의
    풀 자리 div.work.box). WeasyPrint의 레이아웃 단위는 CSS px(96/in)라 pt로 바꾼다."""
    k = 72 / 96
    pages = []
    for page in doc.pages:
        boxes = []
        for box in page._page_box.descendants():
            if not hasattr(box, "content_box_x"):
                continue
            el = getattr(box, "element", None)
            cls = (el.get("class") or "") if el is not None else ""
            if getattr(box, "element_tag", None) != "img" and "box" not in cls.split():
                continue
            boxes.append({"x": box.content_box_x() * k, "y": box.content_box_y() * k,
                          "w": box.width * k, "h": box.height * k, "class": cls})
        pages.append(boxes)
    return pages
```

- [ ] **Step 2: 연마 견본을 다시 빌드해 아무것도 안 바뀌는지 본다**

```bash
python .claude/skills/surisomath-grind/templates/build.py .claude/skills/surisomath-grind/templates/sample/problems.yaml --no-figures
git status --short .claude/skills/surisomath-grind/templates/sample/
```
Expected: 빌드가 `그리드 검사: … 이상 없음`으로 끝나고 종료 코드 0. git status가 비어 있다(같은 입력이면 PDF도 바이트 단위로 같다).

- [ ] **Step 3: --boxes가 box 요소를 적는지 본다**

임시 HTML을 스크래치에 만들어 확인한다.

```bash
mkdir -p "$TMP/boxtest" && cat > "$TMP/boxtest/t.html" <<'EOF'
<!doctype html><html lang="ko"><head><meta charset="utf-8"><title>t</title></head>
<body><p>첫 줄</p><div class="work box"></div><p>둘째 줄</p></body></html>
EOF
python .claude/skills/surisomath-a4/templates/render.py "$TMP/boxtest/t.html" --boxes "$TMP/boxtest/b.json"
python -c "import json,sys; b=json.load(open(sys.argv[1],encoding='utf-8')); print(b)" "$TMP/boxtest/b.json"
```
Expected: `[[{'x': 60.0, 'y': 94.0, 'w': 475.0, 'h': 0.0, 'class': 'work box'}]]` — 본문 위 72pt에 첫 문단 22pt를 더한 94pt(px→pt 환산이라 93.99…로 찍혀도 된다). img가 없으니 상자는 이것 하나다. (`$TMP`는 스크래치 폴더.)

- [ ] **Step 4: 커밋**

```bash
git add .claude/skills/surisomath-a4/templates/render.py
git commit -m "surisomath-a4: --boxes에 class box 요소의 상자도 적는다"
```

---

### Task 2: grind_figure.py에 삼각비 도우미를 보탠다

**Files:**
- Modify: `.claude/skills/surisomath-grind/templates/grind_figure.py` (seg·poly 교체, 상수와 함수 추가)
- Modify: `.claude/skills/surisomath-grind/SKILL.md` (그림 절·쓰는 법)

- [ ] **Step 1: 상수를 더한다**

`DASH = (0, (2, 2))` 줄 아래에 넣는다.

```python
DASHDOT = (6.0, 2.0, 1.0, 2.0)   # 일점쇄선(pt): 긴 획 6, 빈 2, 점 1, 빈 2. 회전축
TICK = 5.0             # 같은 길이 표시 획의 길이(pt)
```

- [ ] **Step 2: seg와 poly를 점선을 받게 바꾼다**

기존 `seg`와 `poly`를 이것으로 교체한다. `dashed()`는 그대로 둔다.

```python
def seg(ax, p, q, lw=EDGE, dashed=False):
    """선분. dashed면 점선 도형의 변 — 대시 2pt 간격 2pt. matplotlib이 대시 길이에
    선 두께를 곱하므로 나눠서 준다."""
    style = (dict(linestyle=(0, (2 / lw, 2 / lw)), dash_capstyle="butt") if dashed
             else dict(solid_capstyle="round"))
    ax.plot([p[0], q[0]], [p[1], q[1]], color=INK, linewidth=lw, **style)
```

```python
def poly(ax, pts, lw=EDGE, dashed=False):
    """꼭짓점 목록으로 닫힌 다각형(삼각형 등)을 그린다. dashed면 점선 도형(007의 A'BC').
    목록을 그대로 돌려준다."""
    for i in range(len(pts)):
        seg(ax, pts[i], pts[(i + 1) % len(pts)], lw=lw, dashed=dashed)
    return list(pts)
```

- [ ] **Step 3: angle · tick · axis · name을 더한다**

`leader()` 아래, 파일 끝에 넣는다.

```python
# ── 각 · 같은 길이 · 회전축 · 이름 ──────────────────────────────────────

def angle(ax, v, p, q, text="", r=12.0, ticks=0, lw=AUX, pad=3.0):
    """각 표시. 꼭짓점 v에서 p 방향부터 q 방향까지(반시계) 반지름 r pt의 호를 긋고,
    글을 각 안쪽 이등분선 위, 호에서 pad pt 떨어진 자리에 앉힌다. 각도 글은
    '$35^{\\circ}$'처럼 수식으로 준다(cmr10에는 °가 없다). ticks가 n이면 호를 가로지르는
    짧은 획 n개 — 같은 각 표시(각의 이등분선). 글 없이 호만 그리려면 text=""."""
    t1 = math.degrees(math.atan2(p[1] - v[1], p[0] - v[0]))
    t2 = math.degrees(math.atan2(q[1] - v[1], q[0] - v[0]))
    while t2 <= t1:
        t2 += 360.0
    R = pt(ax, r)
    ax.add_patch(Arc(v, 2 * R, 2 * R, angle=0, theta1=t1, theta2=t2, linewidth=lw, color=INK))
    m = math.radians((t1 + t2) / 2)
    ux, uy = math.cos(m), math.sin(m)
    s = pt(ax, 2.0)
    for i in range(ticks):
        a = m + math.radians(6.0) * (i - (ticks - 1) / 2)
        cx, cy = math.cos(a), math.sin(a)
        seg(ax, (v[0] + (R - s) * cx, v[1] + (R - s) * cy),
            (v[0] + (R + s) * cx, v[1] + (R + s) * cy), lw=lw)
    if text:
        w, h = text_size(ax, text)
        d = r + pad + (abs(ux) * w + abs(uy) * h) / 2
        label(ax, v[0] + pt(ax, d) * ux, v[1] + pt(ax, d) * uy, text)


def tick(ax, p, q, n=1, lw=AUX):
    """같은 길이 표시. 선분 pq 한가운데에 선분과 직각인 짧은 획(TICK pt) n개, 획 사이 2pt.
    한 쌍은 n=1, 다른 쌍은 n=2로 구분한다(중점 표시)."""
    dx, dy = q[0] - p[0], q[1] - p[1]
    L = math.hypot(dx, dy)
    ux, uy = dx / L, dy / L
    nx, ny = -uy, ux
    half, step = pt(ax, TICK) / 2, pt(ax, 2.0)
    mx, my = (p[0] + q[0]) / 2, (p[1] + q[1]) / 2
    for i in range(n):
        k = (i - (n - 1) / 2) * step
        cx, cy = mx + ux * k, my + uy * k
        seg(ax, (cx - nx * half, cy - ny * half), (cx + nx * half, cy + ny * half), lw=lw)


def axis(ax, p, q, text="", lw=AUX):
    """회전축. 교과서대로 일점쇄선 0.4pt. text(축 이름 '$l$')는 q 끝의 오른쪽 3pt."""
    ax.plot([p[0], q[0]], [p[1], q[1]], color=INK, linewidth=lw,
            linestyle=(0, tuple(d / lw for d in DASHDOT)), dash_capstyle="butt")
    if text:
        label(ax, q[0] + pt(ax, 3.0), q[1], text, ha="left", va="center")


def name(ax, p, s, dx=0.0, dy=0.0, ha="center", va="center"):
    """점 이름·변의 길이 글. p에서 (dx, dy) pt 떨어진 자리에 10pt 정체로 앉힌다.
    변의 길이는 교과서처럼 변 옆에 바로 적는다 — 그려진 변에는 dim(점선 곡선)을 쓰지 않는다."""
    label(ax, p[0] + pt(ax, dx), p[1] + pt(ax, dy), s, ha=ha, va=va)
```

- [ ] **Step 4: 도우미가 그려지는지 스크래치에서 확인한다**

`$TMP/helpers.py`를 만들어 실행한다(역슬래시가 든 파일은 heredoc이 아니라 편집기로 쓴다).

```python
import sys
from pathlib import Path
sys.path.insert(0, r"C:\허문영\surisomath\.claude\skills\surisomath-grind\templates")
import grind_figure as g
g.OUT = Path(__file__).resolve().parent / "figures"
g.OUT.mkdir(exist_ok=True)
f, ax = g.canvas(5, -1.0, 5.0)
A, B, C = (0.0, 4.0), (0.0, 0.0), (5.0, 0.0)
g.poly(ax, [A, B, C])
g.poly(ax, [(0.0, 2.8), B, (7.0, 0.0)], dashed=True)
g.angle(ax, C, A, B, "$30^{\\circ}$", r=12)
g.angle(ax, A, B, C, "", r=12, ticks=1)
g.tick(ax, B, C, n=2)
g.axis(ax, (-1.0, -0.8), (-1.0, 4.6), "$l$")
g.name(ax, A, "A", dx=-4, ha="right")
g.save(f, "h.svg")
svg = (g.OUT / "h.svg").read_text(encoding="utf-8")
assert "stroke-dasharray: 2,2" in svg, "점선 도형"
assert "stroke-dasharray: 6,2,1,2" in svg, "일점쇄선"
print("ok")
```

```bash
python "$TMP/helpers.py"
```
Expected: `h.svg  (…×110pt)` 뒤에 `ok`. (여백 경고 `!`는 나도 된다 — 시험용 그림이다.)

- [ ] **Step 5: 연마 견본을 다시 빌드해 아무것도 안 바뀌는지 본다**

```bash
python .claude/skills/surisomath-grind/templates/build.py .claude/skills/surisomath-grind/templates/sample/problems.yaml
git status --short .claude/skills/surisomath-grind/templates/sample/ build/연마/
```
Expected: 종료 코드 0, git status 비어 있음. seg의 기본값(solid, round cap)이 그대로라 기존 그림은 바이트 단위로 같다.

- [ ] **Step 6: SKILL.md 그림 절에 적는다**

`.claude/skills/surisomath-grind/SKILL.md`의 `## 그림` 절, "지시선: …" 줄 아래에 넣는다.

```
각 표시: `g.angle(ax, v, p, q, "$35^{\circ}$", r=12)`. 꼭짓점 v에서 p 방향부터 q 방향까지 반시계로 0.4pt 호, 글은 각 안쪽 이등분선 위. 각도 글은 수식으로(`^{\circ}` 중괄호 필수). `ticks=1`이면 호를 가로지르는 획 — 같은 각 표시
같은 길이 표시: `g.tick(ax, p, q, n=1)`. 선분 한가운데에 직각인 5pt 획 n개. 다른 쌍은 n=2
점선 도형: `g.poly(ax, pts, dashed=True)`·`g.seg(ax, p, q, dashed=True)`. 0.7pt, 대시 2pt 간격 2pt. 길이 표시의 점선(0.4pt)과 다르다
회전축: `g.axis(ax, p, q, "$l$")`. 교과서대로 일점쇄선 0.4pt. 이름은 q 끝 오른쪽
점 이름·변의 길이 글: `g.name(ax, p, "A", dx=-4, dy=-4, ha="right", va="top")`. p에서 pt로 띄운 자리. 그려진 변의 길이는 교과서처럼 변 옆에 이것으로 적고, dim(점선 곡선)은 그려지지 않은 길이(반지름 등)에 쓴다
```

`## 쓰는 법`의 "지시선: …" 줄 아래에도 한 줄.

```
각·같은 길이·점선 도형·회전축·이름: `g.angle` `g.tick` `g.poly(…, dashed=True)` `g.axis` `g.name` — 그림 절 참고
```

- [ ] **Step 7: 커밋**

```bash
git add .claude/skills/surisomath-grind/templates/grind_figure.py .claude/skills/surisomath-grind/SKILL.md
git commit -m "surisomath-grind: 각·같은 길이·점선 도형·회전축·이름 도우미"
```

---

### Task 3: surisomath-exam 스킬 — exam.css

**Files:**
- Create: `.claude/skills/surisomath-exam/templates/exam.css`

- [ ] **Step 1: exam.css를 쓴다**

```css
/* ==========================================================================
   수리소 수학학원 시험대비 학습지 양식
   surisomath-a4/templates/base.css 위에 얹는다. 판형·여백·그리드·서체는 그쪽 것.
   --------------------------------------------------------------------------
   한 쪽에 문제 둘. 본문 영역 475×682pt(31칸)를 세로 플렉스로 잡아 머리줄을 위에
   두고, 남은 높이(44~682pt)를 두 단이 채운다. 단마다 번호·지문·그림·선지가 위에서
   부터 쌓이고 그 아래가 풀 자리(빈 종이)다.

   쪽의 세로 짜임(본문 영역 위 끝 기준)
     0~11    빈 반 칸
     11~33   머리줄(시리즈 · 시험 · 학년 · 단원). 첫 쪽 오른쪽 끝에 이름 칸 — 캡션 글자, 본문 위상
     33~44   빈 반 칸
     44~66   문제 번호 — typo-4, 제목 위상
     77~     지문, 그림, 선지, 풀 자리
   반 칸을 두 번 비우는 까닭은 연마와 같다. 11 + 22 + 11 + 22 + 11 = 77.
   ========================================================================== */

section.exam {
  box-sizing: border-box; height: 682pt; padding-top: 11pt; margin: 0;
  break-after: page; position: relative;
  display: flex; flex-direction: column;
}
section.exam > * { flex: none; }

/* 머리줄 — 캡션 서체지만 검정 */
section.exam p.series {
  margin: 0 0 11pt; font-size: 10pt; font-weight: 300; letter-spacing: -0.02em;
  line-height: 22pt; color: var(--neutral-1000);
}

/* 이름 칸 — 첫 쪽 머리줄 오른쪽 끝. 빈 자리는 너비를 가진 inline-block(&nbsp;는
   render.py의 어절 처리에 접힌다). 높이 0인 상자의 아래 선이라 글줄 상자를 안 키운다 */
section.exam p.name {
  position: absolute; top: 11pt; right: 0; margin: 0;
  font-size: 10pt; font-weight: 300; letter-spacing: -0.02em; line-height: 22pt;
}
section.exam p.name .blank {
  display: inline-block; width: 90pt; height: 0; margin-left: 4pt;
  border-bottom: 0.4pt solid var(--neutral-1000); vertical-align: -2pt;
}

/* 두 단. 가운데 세로선 하나 — 오른 단의 왼쪽 선이다. x = 237pt(쪽 기준 297pt) 정수 좌표.
   반반(237.5)이면 화면에서 회색으로 보인다. 글은 선에서 8pt. 왼 단 글 너비 229pt,
   오른 단 230pt(선 0.4pt를 빼고 7.6pt 띄워 245pt에서 시작). 세로선은 .cols가 바닥까지
   늘어나고(flex: 1) 단이 그 높이로 늘어나(stretch) 44~682pt를 다 지난다 */
section.exam .cols { flex: 1; display: flex; margin: 0; min-height: 0; }
section.exam .col {
  flex: none; box-sizing: border-box; width: 237pt; min-width: 0;
  padding: 0 8pt 0 0;
}
section.exam .col + .col {
  width: 238pt; padding: 0 0 0 7.6pt;
  border-left: 0.4pt solid var(--neutral-1000);
}

/* 문제 번호 001, 002, … — typo-4. 줄 높이·아래 여백은 h2 그대로라 위상은 변하지 않는다 */
section.exam h2 {
  font-size: 20pt; font-weight: 700; letter-spacing: -0.05em; text-align: left;
}

/* 본문 안 수식. build.py가 $…$를 Computer Modern SVG로 바꿔 넣는다(연마와 같다) */
section.exam img.mi { width: auto; }

/* 선지 — 수능·모의고사 관례의 5열·3열·1열. build.py가 너비를 재서 c5·c3·c1을 고른다.
   칸 너비가 같아 ④가 ① 아래 온다. 항목 한 줄이 한 칸(22pt). 마커 칸은 base.css의 ol --pad */
section.exam ol.n7 { display: flex; flex-wrap: wrap; }
section.exam ol.n7 > li { flex: none; box-sizing: border-box; width: 100%; }
section.exam ol.n7.c5 > li { width: 20%; }
section.exam ol.n7.c3 > li { width: 33.333%; }

/* 풀 자리 — 학생 쪽은 빈 종이. 선생님 쪽은 첫 줄에 정답(캡션 서체·회색), 그 아래 풀이.
   줄마다 p 하나, 문단 간격 없이 한 줄 = 한 칸. 빈 줄은 p.gap. 풀 자리 위 끝은 본문 위상이라
   10pt 정답 줄이 머리줄과 같은 그리드 잔여값에 앉는다 */
section.exam .work { margin: 0; }
section.exam .work p { margin: 0; }
section.exam .work p.gap { height: 22pt; }
section.exam .work p.answer {
  font-size: 10pt; font-weight: 300; letter-spacing: -0.02em; color: var(--c-caption);
}
```

- [ ] **Step 2: 커밋은 Task 4와 함께 한다**

---

### Task 4: surisomath-exam 스킬 — build.py

**Files:**
- Create: `.claude/skills/surisomath-exam/templates/build.py`

- [ ] **Step 1: build.py를 쓴다**

```python
# -*- coding: utf-8 -*-
"""시험대비 학습지 빌드 — problems.yaml 하나로 그림·수식·HTML·PDF·검사까지.

    python build.py 단원폴더/problems.yaml              → 같은 폴더에 HTML과 PDF
    python build.py 단원폴더/problems.yaml --no-figures   그림은 다시 그리지 않는다

하는 일
  1. 단원 폴더에 figures.py가 있으면 실행해 figures/ 에 SVG를 뽑는다. grind_figure를
     찾도록 연마 templates 폴더를 PYTHONPATH에 넣어 준다.
  2. problems.yaml을 읽어 한 쪽에 문제 둘(두 단, 단마다 하나)인 HTML을 쓴다. 그림은 SVG
     높이가 22의 배수인지, 너비가 단 글 너비(229pt) 안인지 본다. 칸 수는 높이가 정한다.
  3. 지문·선지·정답·풀이의 $…$는 연마 build.py의 rich()가 Computer Modern SVG로 그린다.
  4. 선지는 너비를 재서 5열·3열·1열을 고른다(수능·모의고사 관례).
  5. solution이 있는 문제가 하나라도 있으면 학생 쪽 뒤에 선생님 쪽을 같은 짝으로 붙인다.
     풀 자리 첫 줄에 정답, 그 아래 풀이. solution이 없는 문제는 그 단이 빈다.
  6. surisomath-a4/templates/render.py 로 PDF를 뽑고 --check 로 그리드를 검사한다.
  7. 풀 자리의 위 끝(div.work.box)을 레이아웃에서 받아 단마다 남은 칸을 알려 준다. 단을
     넘치면, 풀 자리가 8칸 아래면, 정답 줄이 단보다 넓으면, 풀이가 단 바닥을 넘으면,
     잇단 줄의 수식이 겹치면 경고.

경고가 하나라도 있으면 종료 코드가 1이다. PDF는 그래도 나온다.

problems.yaml
  exam: 2026학년도 2학기 중간고사   # 생략하면 시험 폴더 이름(20262학기중간)에서 만든다
  grade: 중3                       # 생략하면 학년 폴더 이름
  unit: 삼각비                      # 생략하면 단원 폴더 이름
  file: 수리소_시험대비_…            # 생략하면 수리소_시험대비_2026_2학기중간_중3_삼각비
  date: 2026.09.01                 # PDF의 만든 날짜. 생략하면 학기 첫날(1학기 3. 1., 2학기 9. 1.)
  problems:
    - text: 다음 그림과 같이 …       # 지문. 표기 규칙은 SKILL.md. ": "가 들어가면 따옴표로 감싼다
      figure: p1.svg               # figures/ 안의 그림. units는 없다 — SVG 높이가 칸 수를 정한다
      alt: …                       # 그림 설명. PDF에는 안 찍힌다
      choices:                     # 있으면 객관식. 다섯 개
        - $\\dfrac{80}{\\tan 52^{\\circ}+\\tan 35^{\\circ}}$
      answer: ④                    # 정답. 객관식은 번호만, 서술형은 단위까지
      solution: |                  # 선생님 풀이. 줄마다 한 칸. 빈 줄은 한 칸을 비운다
        주어진 그림에서 …
    - "no": "004"                  # 번호를 직접 줄 때. 키까지 따옴표로(YAML은 no를 거짓으로 읽는다)
"""
from __future__ import annotations

import argparse
import calendar
import html
import importlib.util
import json
import os
import re
import subprocess
import sys
import tempfile
from pathlib import Path

import yaml

HERE = Path(__file__).resolve().parent
SKILLS = HERE.parents[1]
A4 = SKILLS / "surisomath-a4" / "templates"
GRIND = SKILLS / "surisomath-grind" / "templates"
RENDER = A4 / "render.py"
BASE_CSS = A4 / "base.css"
EXAM_CSS = HERE / "exam.css"
KOPUB = A4 / "fonts" / "KoPubWorld-Batang-Light.otf"

sys.path.insert(0, str(GRIND))
import grind_figure as g  # noqa: E402  본문 안 수식을 그린다


def _grind_build():
    """연마 build.py를 모듈로 읽는다(__main__ 가드가 있다). 수식 조판(rich)·상대 경로·
    SVG 높이·수식 겹침 검사를 빌려 쓴다. 같은 코드를 복사하지 않는다."""
    spec = importlib.util.spec_from_file_location("grind_build", GRIND / "build.py")
    mod = importlib.util.module_from_spec(spec)
    spec.loader.exec_module(mod)
    return mod


gb = _grind_build()
rich, rel, svg_height, check_overlap = gb.rich, gb.rel, gb.svg_height, gb.check_overlap

SERIES = "시험대비"
GRID = 22.0
BOTTOM = 754.0         # 본문 영역 아래 끝(842 - 88). 풀 자리가 여기서 끝난다
COL_W = 229.0          # 단 글 너비(pt). 왼 단 229, 오른 단 230 — 좁은 쪽으로 잰다
MARK_W = 22.0          # 선지 마커 칸(a4의 ol --pad)
MIN_ROWS = 8           # 풀 자리 최소 칸 수
CAPTION = "#636363"    # neutral-500. 정답 줄
MATH = re.compile(r"\$([^$]+)\$")
FOLDER = re.compile(r"(\d{4})(\d)학기(중간|기말)")
_KOPUB = None


# ── 너비 재기 ───────────────────────────────────────────────────────────

def kopub_width(s: str, size: float) -> float:
    """KoPub 바탕 Light로 조판한 글의 너비(pt). 자간은 a4 규격(12pt -0.03em, 10pt -0.02em)."""
    global _KOPUB
    if _KOPUB is None:
        from fontTools.ttLib import TTFont
        f = TTFont(str(KOPUB))
        _KOPUB = (f.getBestCmap(), f["hmtx"], f["head"].unitsPerEm)
    cmap, hmtx, upem = _KOPUB
    track = -0.02 if size < 12 else -0.03
    w = 0.0
    for ch in s:
        gid = cmap.get(ord(ch))
        w += (hmtx[gid][0] if gid else upem / 2) / upem * size + track * size
    return w


def width_of(s: str, size: float = 12.0) -> float:
    """조판한 글의 너비(pt). $…$는 Computer Modern 잉크 너비, 나머지는 KoPub 폭."""
    from matplotlib.font_manager import FontProperties
    from matplotlib.textpath import TextPath
    w = 0.0
    for i, piece in enumerate(MATH.split(s)):
        if not piece:
            continue
        if i % 2:                                   # 홀수째 조각이 수식이다
            x0, _, x1, _ = TextPath((0, 0), f"${piece}$", size=size,
                                    prop=FontProperties(size=size)).get_extents().extents
            w += float(x1 - x0) + 1.0
        else:
            w += kopub_width(piece, size)
    return w


def choice_cols(choices: list) -> int:
    """선지 열 수. 다섯이 한 줄에 들면 5열, 셋씩 들면 3열, 아니면 1열. 칸 너비가 같아
    ④가 ① 아래 온다. 한 칸 = 마커 칸 22pt + 글 너비."""
    widths = [MARK_W + width_of(str(c).strip()) for c in choices]
    for n in (5, 3):
        if all(w <= COL_W / n for w in widths):
            return n
    return 1


# ── 제목 · 파일 이름 · 날짜 ─────────────────────────────────────────────

def meta(data: dict, out_dir: Path) -> dict:
    """머리줄·파일 이름·PDF 날짜. 폴더 build/시험대비/<시험>/<학년>/<단원>/ 에서 만들고
    yaml의 exam·grade·unit·file·date가 있으면 그것이 이긴다."""
    m = FOLDER.fullmatch(out_dir.parents[1].name) if len(out_dir.parents) > 1 else None
    exam = data.get("exam") or (m and f"{m[1]}학년도 {m[2]}학기 {m[3]}고사")
    grade = data.get("grade") or (m and out_dir.parent.name)
    unit = data.get("unit") or (m and out_dir.name)
    if not (exam and grade and unit):
        raise SystemExit("폴더가 build/시험대비/<YYYY><N>학기<중간|기말>/<학년>/<단원>/ 꼴이 아니다. "
                         "problems.yaml에 exam·grade·unit을 적어라")
    if m:
        stem = f"수리소_시험대비_{m[1]}_{m[2]}학기{m[3]}_{grade}_{unit}"
        epoch = calendar.timegm((int(m[1]), 3 if m[2] == "1" else 9, 1, 0, 0, 0))
    else:
        stem = f"수리소_시험대비_{exam}_{grade}_{unit}".replace(" ", "")
        epoch = None
    if data.get("date"):
        d = re.fullmatch(r"(\d{4})\.(\d{1,2})\.(\d{1,2})", str(data["date"]).strip())
        if not d:
            raise SystemExit(f"date는 YYYY.MM.DD 꼴로 적어라: {data['date']}")
        epoch = calendar.timegm((int(d[1]), int(d[2]), int(d[3]), 0, 0, 0))
    return {"exam": str(exam), "grade": str(grade), "unit": str(unit),
            "file": str(data.get("file") or stem), "epoch": epoch,
            "head": f"{SERIES} · {exam} · {grade} · {unit}"}


# ── HTML ────────────────────────────────────────────────────────────────

def svg_width(path: Path) -> float | None:
    m = re.search(r'<svg[^>]*\swidth="([\d.]+)pt"', path.read_text(encoding="utf-8")[:4000])
    return float(m.group(1)) if m else None


def figure_html(no: str, p: dict, fig_dir: Path) -> str:
    if not p.get("figure"):
        return ""
    svg = fig_dir / str(p["figure"])
    if not svg.is_file():
        raise SystemExit(f"{no}: 그림 파일이 없다: {svg}")
    h, w = svg_height(svg), svg_width(svg)
    if h is None or w is None:
        raise SystemExit(f"{no}: {svg.name}의 크기를 읽을 수 없다. grind_figure.save()로 만든 SVG여야 한다")
    units = round(h / GRID)
    if abs(h - units * GRID) > 0.05:
        raise SystemExit(f"{no}: {svg.name} 높이 {h:g}pt가 22의 배수가 아니다. g.canvas의 units를 보라")
    if w > COL_W:
        raise SystemExit(f"{no}: {svg.name} 너비 {w:.0f}pt가 단 글 너비 {COL_W:.0f}pt를 넘는다. "
                         f"y 범위를 {w / COL_W:.2f}배 넓혀 배율을 줄여라")
    alt = html.escape(str(p.get("alt", "")))
    return (f'      <div class="figure" style="--u:{units}">'
            f'<img src="figures/{svg.name}" alt="{alt}"></div>\n')


def choices_html(no: str, p: dict, fig_dir: Path) -> str:
    choices = p.get("choices")
    if not choices:
        return ""
    cols = choice_cols(choices)
    items = "".join(f'        <li>{rich(str(c).strip(), f"c{no}_{k}", fig_dir, 12.0, g.INK)}</li>\n'
                    for k, c in enumerate(choices, 1))
    return f'      <ol class="n7 c{cols}">\n{items}      </ol>\n'


def work_html(no: str, p: dict, fig_dir: Path) -> str:
    """선생님 쪽 풀 자리. 첫 줄 정답(10pt 회색), 그 아래 풀이 한 줄 = 한 칸. 빈 줄은 p.gap."""
    if not p.get("solution"):
        return ""
    out = [f'        <p class="answer">정답: '
           f'{rich(str(p["answer"]).strip(), f"a{no}", fig_dir, 10.0, CAPTION)}</p>']
    for i, line in enumerate(str(p["solution"]).strip("\n").split("\n"), 1):
        if not line.strip():
            out.append('        <p class="gap"></p>')
        else:
            out.append(f'        <p>{rich(line.strip(), f"s{no}_{i}", fig_dir, 12.0, g.INK)}</p>')
    return "\n".join(out) + "\n"


def problem_html(no: str, p: dict, fig_dir: Path, teacher: bool) -> str:
    text = rich(str(p["text"]).strip(), f"m{no}", fig_dir, 12.0, g.INK)
    work = work_html(no, p, fig_dir) if teacher else ""
    return ('    <div class="col">\n'
            f'      <h2>{no}</h2>\n'
            f'      <p>{text}</p>\n'
            f'{figure_html(no, p, fig_dir)}'
            f'{choices_html(no, p, fig_dir)}'
            f'      <div class="work box">\n{work}      </div>\n'
            '    </div>\n')


def page_html(pair: list, head: str, name: bool, fig_dir: Path, teacher: bool) -> str:
    cols = "".join(problem_html(no, p, fig_dir, teacher) for no, p in pair)
    if len(pair) == 1:                          # 마지막 쪽에 문제가 하나면 오른 단은 비운다
        cols += '    <div class="col"></div>\n'
    name_html = '  <p class="name">이름<span class="blank"></span></p>\n' if name else ""
    return ('<section class="exam">\n'
            f'  <p class="series">{html.escape(head)}</p>\n'
            f'{name_html}'
            '  <div class="cols">\n'
            f'{cols}'
            '  </div>\n'
            '</section>\n')


def numbers(problems: list) -> list[str]:
    return [str(p.get("no") or f"{i:03d}") for i, p in enumerate(problems, 1)]


def pages(problems: list) -> list[list]:
    items = list(zip(numbers(problems), problems))
    return [items[i:i + 2] for i in range(0, len(items), 2)]


def build_html(data: dict, m: dict, out_dir: Path) -> tuple[str, list[str], int]:
    """HTML 전체, 쪽마다 붙일 이름표(경고에 쓴다), 학생 쪽 수."""
    problems = data.get("problems") or []
    fig_dir = out_dir / "figures"
    body, labels = [], []
    for i, pair in enumerate(pages(problems)):
        body.append(page_html(pair, m["head"], i == 0, fig_dir, teacher=False))
        labels.append("·".join(no for no, _ in pair))
    n_student = len(body)
    if any(p.get("solution") for p in problems):
        for pair in pages(problems):
            body.append(page_html(pair, m["head"], False, fig_dir, teacher=True))
            labels.append("선생님 " + "·".join(no for no, _ in pair))
    doc = ('<!doctype html>\n<html lang="ko">\n<head>\n<meta charset="utf-8">\n'
           f'<title>{html.escape(m["head"])}</title>\n'
           f'<link rel="stylesheet" href="{rel(BASE_CSS, out_dir)}">\n'
           f'<link rel="stylesheet" href="{rel(EXAM_CSS, out_dir)}">\n'
           '</head>\n<body>\n\n' + "\n".join(body) + '\n</body>\n</html>\n')
    return doc, labels, n_student


# ── 검사 ────────────────────────────────────────────────────────────────

def check(problems: list) -> int:
    """yaml에서 흔히 나는 잘못을 문제 번호와 함께 알려 준다. 정답 줄이 단보다 넓으면 경고."""
    if not problems:
        raise SystemExit("problems가 비어 있다")
    bad = 0
    for i, p in enumerate(problems, 1):
        if not isinstance(p, dict):
            raise SystemExit(f"{i}번째 문제가 표가 아니다. '- text: …' 꼴로 적어라")
        # YAML 1.1은 따옴표 없는 no를 거짓으로 읽는다. 조용히 번호가 밀리므로 여기서 잡는다
        if False in p:
            raise SystemExit(f'{i}번째 문제: no를 "no": "{p[False]}" 로 적어라. '
                             "따옴표가 없으면 YAML이 거짓으로 읽어 번호가 무시된다")
        for key in ("text", "answer"):
            if key not in p:
                raise SystemExit(f"{i}번째 문제에 {key}가 없다")
        if "choices" in p and len(p["choices"] or []) != 5:
            raise SystemExit(f"{i}번째 문제: 선지는 다섯 개여야 한다")
        if p.get("solution"):
            w = width_of("정답: " + str(p["answer"]).strip(), 10.0)
            if w > COL_W:
                print(f"  ! {i}번째 문제: 정답 줄 {w:.0f}pt가 단 글 너비 {COL_W:.0f}pt를 넘는다. 짧게 써라")
                bad += 1
    return bad


def report(pdf: Path, boxes: list, labels: list[str], n_student: int) -> int:
    """학생 쪽마다 풀 자리 위 끝(div.work.box)을 받아 남은 칸을 잰다. 단을 넘치면 경고.
    선생님 쪽은 풀이 글이 단 바닥을 넘는지 본다(글자가 본문 영역 아래로 나간다.
    쪽번호 10pt는 뺀다)."""
    bad = 0
    if len(boxes) != len(labels):
        print(f"  ! 쪽 수 {len(boxes)} ≠ {len(labels)}. 어느 문제가 한 쪽을 넘쳤다")
        return 1
    rows = []
    for label, page in zip(labels[:n_student], boxes):
        works = sorted((b for b in page if "work" in b["class"].split()), key=lambda b: b["x"])
        for no, b in zip(label.split("·"), works):
            r = (BOTTOM - b["y"]) / GRID
            if r < 0:
                print(f"  ! {no}: 문제 블록이 단 바닥을 {-r:.1f}칸 넘는다. 그림을 줄이거나 선지를 보라")
                bad += 1
            rows.append((no, r))
    if rows:
        no, r = min(rows, key=lambda t: t[1])
        print(f"  풀 자리: 가장 좁은 단 {no} {r:.1f}칸")
        if 0 <= r < MIN_ROWS:
            print(f"  ! {no}: 풀 자리 {r:.1f}칸. {MIN_ROWS}칸은 두라")
            bad += 1
    if len(labels) > n_student:
        import pdfplumber
        with pdfplumber.open(pdf) as doc:
            for label, page in zip(labels[n_student:], doc.pages[n_student:]):
                nos = label.split(" ", 1)[1].split("·")
                for no, (x0, x1) in zip(nos, ((0, 297), (297, 595))):
                    if any(c["bottom"] > BOTTOM + 0.5 and c["size"] > 10.5 and x0 <= c["x0"] < x1
                           for c in page.chars):
                        print(f"  ! {no}: 풀이 글이 단 바닥을 넘는다. 줄을 줄여라")
                        bad += 1
    return bad


# ── 빌드 ────────────────────────────────────────────────────────────────

def main() -> int:
    if hasattr(sys.stdout, "reconfigure"):
        sys.stdout.reconfigure(encoding="utf-8")     # Windows 콘솔에서 한글이 깨지지 않게
    ap = argparse.ArgumentParser(description="시험대비 학습지 빌드")
    ap.add_argument("source", type=Path, help="problems.yaml")
    ap.add_argument("--no-figures", action="store_true", help="figures.py를 실행하지 않는다")
    ap.add_argument("--no-check", action="store_true", help="그리드 검사를 건너뛴다")
    args = ap.parse_args()

    src = args.source.resolve()
    if not src.is_file():
        print(f"입력 파일이 없다: {src}", file=sys.stderr)
        return 1
    out_dir = src.parent
    try:
        data = yaml.safe_load(src.read_text(encoding="utf-8")) or {}
    except yaml.YAMLError as e:
        mark = getattr(e, "problem_mark", None)
        where = f" ({mark.line + 1}째 줄)" if mark else ""
        print(f"{src.name}을 읽을 수 없다{where}. ': '가 든 글은 따옴표로 감싸라.", file=sys.stderr)
        print(e, file=sys.stderr)
        return 1
    problems = data.get("problems") or []
    warnings = check(problems)
    m = meta(data, out_dir)

    env = dict(os.environ, PYTHONIOENCODING="utf-8",
               PYTHONPATH=os.pathsep.join(p for p in (str(GRIND), os.environ.get("PYTHONPATH")) if p))
    # PDF 안의 만든 날짜를 고정한다(WeasyPrint는 SOURCE_DATE_EPOCH를 따른다). 학기 첫날이거나
    # yaml의 date. 둘 다 없으면 yaml의 수정 시각
    env["SOURCE_DATE_EPOCH"] = str(m["epoch"] if m["epoch"] is not None else int(src.stat().st_mtime))
    figures_py = out_dir / "figures.py"
    if figures_py.is_file() and not args.no_figures:
        r = subprocess.run([sys.executable, str(figures_py)], cwd=str(out_dir), env=env,
                           stdout=subprocess.PIPE, stderr=None, text=True, encoding="utf-8")
        print(r.stdout, end="", flush=True)
        if r.returncode:
            return r.returncode
        warnings += sum(1 for line in r.stdout.splitlines() if line.lstrip().startswith("!"))
    fig_dir = out_dir / "figures"
    fig_dir.mkdir(exist_ok=True)
    # 수식 SVG(m 지문 · c 선지 · a 정답 · s 풀이 + 번호)는 빌드마다 다시 그린다
    for old in fig_dir.glob("*.svg"):
        if re.fullmatch(r"[macs]\d{3}(_\d+)+\.svg", old.name):
            old.unlink()

    doc, labels, n_student = build_html(data, m, out_dir)
    out_html = out_dir / f"{m['file']}.html"
    out_html.write_text(doc, encoding="utf-8")
    print(out_html, flush=True)

    fd, tmp = tempfile.mkstemp(suffix=".json", prefix="exam_boxes_")
    os.close(fd)                       # 열어 둔 채면 Windows에서 지울 수 없다
    boxes_json = Path(tmp)
    cmd = [sys.executable, str(RENDER), str(out_html), "--boxes", str(boxes_json)]
    if not args.no_check:
        cmd.append("--check")
    try:
        r = subprocess.run(cmd, cwd=str(out_dir), env=env)
        if r.returncode:
            return r.returncode
        boxes = json.loads(boxes_json.read_text(encoding="utf-8"))
    finally:
        boxes_json.unlink(missing_ok=True)
    warnings += check_overlap(boxes, labels)
    warnings += report(out_html.with_suffix(".pdf"), boxes, labels, n_student)
    if warnings:
        print(f"  경고 {warnings}개. 위의 ! 줄을 보라")
    return 1 if warnings else 0


if __name__ == "__main__":
    sys.exit(main())
```

- [ ] **Step 2: 순수 함수를 확인한다**

`$TMP/t_build.py`(역슬래시가 있으니 편집기로 쓴다).

```python
import sys
from pathlib import Path
sys.path.insert(0, r"C:\허문영\surisomath\.claude\skills\surisomath-exam\templates")
import build as b

# 선지 열: 짧으면 5열, 분수·긴 식은 1열, 중간은 3열
assert b.choice_cols(["$0$", "$\\dfrac{1}{2}$", "$1$", "$\\dfrac{3}{2}$", "$2$"]) == 5
assert b.choice_cols(["$\\dfrac{80}{\\tan 52^{\\circ}+\\tan 35^{\\circ}}$"] * 5) == 1
assert b.choice_cols(["$0$", "$2\\sin x$", "$2\\cos x$", "$2\\sin x-2\\cos x$", "$2\\cos x-2\\sin x$"]) == 1
assert b.choice_cols(["$\\sqrt{2}$", "$\\sqrt{3}$", "$2\\sqrt{2}$", "$2\\sqrt{3}$", "$3\\sqrt{2}$"]) == 3

# 폴더 이름 → 머리줄·파일 이름·날짜
m = b.meta({}, Path(r"C:\허문영\surisomath\build\시험대비\20262학기중간\중3\삼각비"))
assert m["head"] == "시험대비 · 2026학년도 2학기 중간고사 · 중3 · 삼각비", m["head"]
assert m["file"] == "수리소_시험대비_2026_2학기중간_중3_삼각비", m["file"]
import calendar; assert m["epoch"] == calendar.timegm((2026, 9, 1, 0, 0, 0))
m = b.meta({"exam": "견본", "grade": "중3", "unit": "삼각비", "file": "수리소_시험대비_견본", "date": "2026.09.14"},
           Path(r"C:\허문영\surisomath\.claude\skills\surisomath-exam\templates\sample"))
assert m["head"] == "시험대비 · 견본 · 중3 · 삼각비" and m["file"] == "수리소_시험대비_견본"
assert m["epoch"] == calendar.timegm((2026, 9, 14, 0, 0, 0))

# 너비: KoPub 바탕 한글 한 자는 0.936em이라 12pt에서 자간을 빼면 10.9pt
w = b.kopub_width("가", 12.0)
assert 10.5 < w < 11.5, w
print("ok")
```

```bash
python "$TMP/t_build.py"
```
Expected: `ok`. (모듈 이름이 build라 `import build`로 불러진다. 3열 검사가 실패하면 √ 선지의 너비를 `b.width_of("$2\\sqrt{3}$")`로 찍어 보고 76.3pt 안인지 본다 — 22 + 글 너비가 기준이다.)

- [ ] **Step 3: 커밋(exam.css와 함께)**

```bash
git add .claude/skills/surisomath-exam/templates/exam.css .claude/skills/surisomath-exam/templates/build.py
git commit -m "surisomath-exam: 시험대비 양식 스타일과 빌드"
```

---

### Task 5: 견본 단원

**Files:**
- Create: `.claude/skills/surisomath-exam/templates/sample/problems.md`
- Create: `.claude/skills/surisomath-exam/templates/sample/problems.yaml`
- Create: `.claude/skills/surisomath-exam/templates/sample/figures.py`
- Build output: `sample/figures/`, `sample/수리소_시험대비_견본.html`, `sample/수리소_시험대비_견본.pdf` (연마 견본처럼 함께 커밋한다)

- [ ] **Step 1: problems.md**

```markdown
# 견본


## 001


### 지문
sin 30° + cos 60°의 값은?


### 선지
1. 0
2. 1/2
3. 1
4. 3/2
5. 2


## 002


### 지문
다음 그림과 같이 각 B = 90°, 각 A = 30°, 선분 AC = 10인 직각삼각형 ABC에서 선분 BC의 길이를 구하시오.


### 그림
각 B가 직각, 각 A가 30°, 선분 AC의 길이가 10인 직각삼각형
```

- [ ] **Step 2: problems.yaml**

```yaml
# 시험대비 견본. 새 단원을 만들 때 이 폴더의 problems.yaml과 figures.py를 복사해 출발한다.
#
#   exam · grade · unit · file · date  정본 폴더(build/시험대비/<시험>/<학년>/<단원>/)에서는 다섯 줄 다
#                                      지운다. build.py가 폴더 이름에서 만든다. 견본은 폴더 꼴이 달라 적었다
#   problems  순서대로 001, 002, … 번호가 붙는다. 번호를 직접 주려면 "no": "004" — 키까지 따옴표로
#     text     지문. problems.md 문장 그대로, 표기만 조판한다(SKILL.md 표기 규칙)
#     figure   figures/ 안의 그림. figures.py가 그린다. units는 적지 않는다 — SVG 높이가 칸 수다
#     alt      그림 설명. PDF에는 안 찍힌다
#     choices  있으면 객관식. 다섯 개. 열 수는 build.py가 너비로 정한다
#     answer   정답. 객관식은 번호만("④"), 서술형은 단위까지
#     solution 선생님 풀이. 줄마다 한 칸. 있으면 뒤에 선생님 쪽이 붙는다

exam: 견본
grade: 중3
unit: 삼각비
file: 수리소_시험대비_견본
date: 2026.09.14

problems:
  - text: $\sin 30^{\circ}+\cos 60^{\circ}$의 값은?
    choices:
      - $0$
      - $\dfrac{1}{2}$
      - $1$
      - $\dfrac{3}{2}$
      - $2$
    answer: ③
    solution: |
      $\sin 30^{\circ}=\dfrac{1}{2}$, $\cos 60^{\circ}=\dfrac{1}{2}$이므로
      $\sin 30^{\circ}+\cos 60^{\circ}=1$이다.
      따라서 구하는 값은 $1$이다.

  - text: 다음 그림과 같이 $\angle\mathrm{B}=90^{\circ}$, $\angle\mathrm{A}=30^{\circ}$, $\overline{\mathrm{AC}}=10$인 직각삼각형 $\mathrm{ABC}$에서 $\overline{\mathrm{BC}}$의 길이를 구하시오.
    figure: p2.svg
    alt: 각 B가 직각, 각 A가 30°, 빗변 AC의 길이가 10인 직각삼각형
    answer: 5
    solution: |
      주어진 그림에서
      $\overline{\mathrm{BC}}=\overline{\mathrm{AC}}\sin 30^{\circ}$이다.
      따라서 $\overline{\mathrm{BC}}=5$이다.
```

- [ ] **Step 3: figures.py**

```python
# -*- coding: utf-8 -*-
"""시험대비 견본 — 문제 그림 한 장. 새 단원의 figures.py는 이 파일을 그대로 복사해 쓴다.

build.py가 grind_figure의 import 경로를 잡아 주므로 이 파일은 build.py로 실행한다.
그림은 단 글 너비(229pt) 안에 들어야 한다. y 범위가 배율을 정하니 넓은 그림은 y 범위를
넉넉히 잡는다. save()가 위아래 여백을 재서 y 범위를 얼마로 잡을지 일러 준다.
"""
from __future__ import annotations

import math

import grind_figure as g

g.setup(__file__)


def p2():
    """∠B = 90°, ∠A = 30°, AC = 10인 직각삼각형. 길이는 교과서처럼 변 옆에 적는다."""
    A = (0.0, 0.0)
    B = (10 * math.cos(math.radians(30)), 0.0)
    C = (B[0], 5.0)
    f, ax = g.canvas(5, -1.0, 6.2)
    g.poly(ax, [A, B, C])
    g.right_angle(ax, B, -1, 1)
    g.angle(ax, A, B, C, "$30^{\\circ}$", r=16)
    g.name(ax, A, "A", dx=-4, dy=-4, ha="right", va="top")
    g.name(ax, B, "B", dx=4, dy=-4, ha="left", va="top")
    g.name(ax, C, "C", dx=4, dy=4, ha="left", va="bottom")
    g.name(ax, ((A[0] + C[0]) / 2, (A[1] + C[1]) / 2), "10", dx=-5, dy=4, ha="right", va="bottom")
    g.save(f, "p2.svg")


if __name__ == "__main__":
    print("figures/")
    p2()
```

- [ ] **Step 4: 빌드한다**

```bash
python .claude/skills/surisomath-exam/templates/build.py .claude/skills/surisomath-exam/templates/sample/problems.yaml
```
Expected(첫 실행): p2.svg의 여백 경고가 `! p2.svg: 위아래 여백 … y 범위를 (a, b)로 잡아라`로 나올 수 있다. 그 값을 `g.canvas(5, a, b)`에 넣고 다시 돌린다. 끝내 이렇게 끝나야 한다.

```
figures/
  p2.svg  (…×110pt)
…\sample\수리소_시험대비_견본.html
…\sample\수리소_시험대비_견본.pdf  (2쪽)
  그리드 검사: N행 중 이상 없음
  풀 자리: 가장 좁은 단 002 17.5칸
```
종료 코드 0. 001은 지문 한 줄 + 선지 5열 한 줄 + 사이·아래 한 칸씩이라 풀 자리 23.5칸, 002는 지문 세 줄 + 그림 5칸 + 사이·아래 한 칸씩이라 17.5칸이다(지문이 네 줄로 떨어지면 16.5).

- [ ] **Step 5: 눈으로 본다**

```bash
python -c "import fitz; d=fitz.open('.claude/skills/surisomath-exam/templates/sample/수리소_시험대비_견본.pdf'); [p.get_pixmap(dpi=110).save(f'$TMP/sample{p.number}.png') for p in d]"
```
두 PNG를 연다. 볼 것: 머리줄 "시험대비 · 견본 · 중3 · 삼각비"와 첫 쪽 오른쪽 끝 "이름 ______", 가운데 세로선이 번호 줄 위부터 바닥까지, 001의 선지 ①~⑤가 한 줄에, 002의 그림이 단 가운데, 2쪽(선생님)의 풀 자리 첫 줄에 회색 "정답: ③"·"정답: 5"와 그 아래 풀이.

- [ ] **Step 6: 커밋**

```bash
git add .claude/skills/surisomath-exam/templates/sample/
git commit -m "surisomath-exam: 견본 두 문제"
```

---

### Task 6: SKILL.md와 README

**Files:**
- Create: `.claude/skills/surisomath-exam/SKILL.md`
- Modify: `README.md:33-52` (surisomath-grind 절 뒤에 surisomath-exam 절)

- [ ] **Step 1: SKILL.md를 쓴다**

```markdown
---
name: surisomath-exam
description: "수리소 수학학원의 실전 모의 학습지 '시험대비' 양식이 작성되어 있는 SKILL입니다. 시험대비 학습지를 새로 만들거나 고치는 상황에 사용합니다. surisomath-a4 위에 얹고 surisomath-grind의 그림 도우미와 수식 조판을 빌려 쓰므로 두 SKILL도 함께 읽어야 합니다. 한 쪽에 문제 둘, 두 단에 한 문제씩이고 문제 아래가 학생이 손으로 푸는 빈 자리입니다."
---

# 수리소 수학학원 시험대비 학습지 양식


## 환경
포맷: PDF
선행 스킬: surisomath-a4 — 판형·여백·그리드·서체·렌더·그리드 검사. surisomath-grind — 그림 도우미(grind_figure.py)와 본문 수식 조판(build.py의 rich), 분수 규격
작업 순서: build.py가 그림 → 수식 → HTML → PDF → 검사를 한 번에 한다. a4의 render.py를 직접 부르지 않는다
패키지: pyyaml, weasyprint, pdfplumber, matplotlib, pymupdf. fontTools는 matplotlib이 깐다


## 쪽
단위: 한 쪽에 문제 둘(`<section class="exam">`). 두 단, 단마다 문제 하나
본문 영역: 475×682pt(31칸). 위에 머리줄, 44pt부터 바닥까지 두 단
학생 쪽: 문제 열 개면 다섯 쪽. 선생님 쪽이 그 뒤에 같은 짝으로 다섯 쪽(6~10쪽)
표지·제목 줄·이름 칸 행: 없음. 머리줄에 시험·학년·단원이 있고 첫 쪽 머리줄 오른쪽 끝에 이름 칸이 있다
쪽번호: a4 그대로(- n -). 선생님 쪽까지 이어 센다


## 쪽의 세로 짜임
기준: 본문 영역 위 끝 = 0
0~11pt: 빈 반 칸
11~33pt: 머리줄 "시험대비 · 2026학년도 2학기 중간고사 · 중3 · 삼각비". typo-0, neutral-1000. 본문 위상(11 + 22k). 첫 쪽만 오른쪽 끝에 "이름"과 90pt 빈 자리(아래 선 0.4pt)
33~44pt: 빈 반 칸
44~66pt: 문제 번호. typo-4. 제목 위상(22k)
66~77pt: 번호 아래 반 칸
77pt~: 지문 → 그림 → 선지. 블록 사이·선지 아래 한 칸씩. 본문 위상
그 아래~682pt: 풀 자리. 빈 종이. 소제목·심볼·가로선 없음
반 칸을 두 번 비우는 까닭: 연마와 같다. 11 + 22 + 11 + 22 + 11 = 77
풀 자리: 문제 글부터 바닥까지 27.5칸에서 문제 블록을 뺀 것. 늘 .5로 끝난다. build.py가 가장 좁은 단을 알려 주고 8칸 아래면 경고한다. 문제 블록이 단을 넘치면 멈춘다


## 단
왼 단 x = 0~237pt, 오른 단 237~475pt. 가운데 세로선 하나(neutral-1000, 0.4pt)가 x = 237pt(쪽 기준 297pt)에 번호 줄 위(44pt)부터 바닥(682pt)까지. 정수 좌표라야 화면에서 검게 보인다
글은 선에서 8pt. 왼 단 글 너비 229pt, 오른 단 230pt
바깥 세로선·가로선: 없음. 학교 시험지 꼴이다. 연마의 세 줄 상자는 두 번 푸는 자리라 여기 안 쓴다


## 문제
번호: 001부터 problems.yaml 차례대로. typo-4, 단 왼끝. 순서는 바꾸지 않는다
지문: typo-1, 한 문단. problems.md 문장을 한 글자도 바꾸지 않는다. 바꾸는 것은 표기뿐이다
그림: 지문 아래 단 가운데. a4의 `.figure` 블록. units는 yaml에 적지 않는다 — SVG 높이가 칸 수다(22의 배수가 아니면 멈춘다). 너비 229pt 안. 넘으면 build.py가 y 범위를 몇 배 넓히라고 알려 준다
선지: `<ol class="n7">`(① ② ③ ④ ⑤, 마커 칸 22pt). build.py가 선지마다 너비(마커 22pt + 글)를 재서 다섯 모두 45.8pt(단 글 너비 ÷ 5) 안이면 5열 한 줄, 76.3pt(÷ 3) 안이면 3열(①②③ / ④⑤, ④는 ① 아래), 아니면 1열 다섯 줄. 수능·모의고사 관례다
넣지 않는 것: 답 칸, 배점, [서술형] 표시. 문제 글에 없는 것은 넣지 않는다


## 표기 규칙
낱말로 쓴 도형 표기를 교과서 기호로 조판한다. 연마 규칙(`!=` → `\neq`, `b/x` → `\dfrac{b}{x}`, 점 이름 정체, 변수 이탤릭, 수식 뒤 조사 붙여 쓰기) 위에 얹는다
선분 AB → `$\overline{\mathrm{AB}}$`
각 A, 각 BAC → `$\angle\mathrm{A}$`, `$\angle\mathrm{BAC}$`
삼각형 ABC → `$\triangle\!\mathrm{ABC}$`. `\!`가 없으면 "△ ABC"처럼 벌어진다
사각형 ABCD → `$□\mathrm{ABCD}$`. 유니코드 □를 그대로 쓴다. `\square`는 mathtext에 없다
루트 (…)^2, 2루트3 → `$\sqrt{(\cdots)^2}$`, `$2\sqrt{3}$`
sinx, tan 52°, 35° → `$\sin x$`, `$\tan 52^{\circ}$`, `$35^{\circ}$`. 각도는 낱말 사이에 홀로 있어도 수식 서체다. `^{\circ}`처럼 중괄호로 감싼다 — `^\circ`는 "60 °"처럼 벌어지고, 수식 안의 °는 γ로 찍힌다
각 BAC = 60°, tan63°=1.96 → `$\angle\mathrm{BAC}=60^{\circ}$`, `$\tan 63^{\circ}=1.96$`
80 / (tan 52° + tan 35°) → `$\dfrac{80}{\tan 52^{\circ}+\tan 35^{\circ}}$`
A'BC' → `$\triangle\!\mathrm{A}'\mathrm{BC}'$`. 프라임은 mathrm 밖에
낱말로 남기는 것: 직각삼각형 ABC, 정사각형 ABCD, 직사각형 ABCD(이름만 `$\mathrm{ABCD}$`), 점 A, 지점 A, 직선 $l$(이탤릭), 중점 M. 교과서에서도 낱말이다
치수: 80m, 4cm, 124m, 30%는 글자 그대로(a4의 단위 붙여쓰기). 수식 안 값에 단위가 붙으면 `$\overline{\mathrm{AB}}=8$cm`처럼 수식 뒤에 글자로
분수 분자에 근호가 들면(`$\dfrac{40\sqrt{3}}{9}$`) 수식이 글줄 상자 위로 3pt쯤 넘는다. 교과서 표기라 그대로 쓰되 잇단 두 줄에서 세로로 겹치게 두지 않는다 — build.py가 겹치면 경고한다


## 선생님 쪽
자리: 학생 쪽 뒤에 같은 짝으로. 양식은 학생 쪽과 똑같고 이름 칸만 없다
정답 줄: 풀 자리 첫 줄 "정답: ④". typo-0, neutral-500, 왼끝. 객관식은 번호만, 서술형은 단위까지(182.35m, 2% 감소). 한 줄, 단 글 너비 안 — 넘으면 경고
풀이: yaml의 `solution: |` 블록. 정답 줄 다음 줄부터. 줄마다 한 칸, 빈 줄은 한 칸을 비운다. typo-1, 수식은 `$…$`. 한 줄이 단 너비(한글 19자쯤)를 안 넘게 짧게 끊어 쓴다. 단 바닥을 넘으면 경고
풀이 문체: 연마 규칙 그대로. 서술형 모범 답안. 전개만 쓰고 대입한 식·계산은 일부러 생략. "~이므로 ~이다." 마지막 줄 "따라서 …이다." 그림은 "주어진 그림에서". 객관식은 마지막 줄에 식을 적고 정답 줄이 번호를 맡는다
solution이 없는 문제: 그 단의 풀 자리가 비고 정답도 안 찍힌다. 문제 전부에 없으면 선생님 쪽을 안 붙인다


## 그림
규격: 연마 것 그대로(도형 선 0.7pt, 보조선 0.4pt, 글자 10pt 정체, 잉크 여백 4pt, y 범위가 배율)
너비: 단이 좁다. 넓은 그림(두 지점에서 올려다본 산)은 y 범위를 넉넉히 잡아 배율을 낮춘다
칸 수: 5~7. 도형 하나면 5, 회전축처럼 세로로 긴 그림은 7
삽화: 없다. 산·기구·성산일출봉은 지면 선 위의 삼각형이다
각: `g.angle(ax, v, p, q, "$35^{\circ}$", r=12)`. 호 0.4pt, 글은 각 안쪽 이등분선 위. 같은 각 표시는 `ticks=1`
같은 길이: `g.tick(ax, p, q, n)`. 중점 표시. 다른 쌍은 n=2
길이 글: 교과서처럼 변 옆에 바로 적는다 — `g.name(ax, 변의 가운데, "80m", dy=-5, va="top")`. 점선 곡선(`g.dim`)은 그려지지 않은 길이에만
점선 도형: `g.poly(ax, pts, dashed=True)`. 회전축: `g.axis(ax, p, q, "$l$")` 일점쇄선
직각 표시: `g.right_angle`·`g.foot`. 정사각형·직사각형은 `g.rect`가 네 귀퉁이에 그린다


## 파일
templates/exam.css: base.css 위에 얹는 시험대비 스타일. 위 규격의 구현
templates/build.py: problems.yaml 하나로 그림 → 수식 → HTML → PDF → 검사. 연마 build.py를 모듈로 읽어 rich()를 쓴다
templates/sample/: 견본 두 문제(객관식 5열, 그림 있는 서술형). 새 단원은 이 폴더의 problems.yaml과 figures.py를 복사해 출발한다. 실제 학습지의 정본은 build/시험대비/20262학기중간/중3/삼각비/


## 단원 폴더
자리: build/시험대비/<시험>/<학년>/<단원>/. 시험 폴더 이름은 `YYYY` + `N학기` + `중간|기말`(20262학기중간). build.py가 정규식으로 읽어 머리줄 "2026학년도 2학기 중간고사"와 파일 이름 수리소_시험대비_2026_2학기중간_중3_삼각비를 만든다. 학년·단원 폴더 이름은 그대로 쓴다
problems.md: 선생님 원문. `## 001` 아래 `### 지문` `### 그림`(설명) `### 선지`(1.~5.). 빌드는 이 파일을 읽지 않고 손대지도 않는다
problems.yaml: 빌드 입력. 지문을 표기 규칙대로 옮기고 choices·answer·solution, 그림이 있으면 figure·alt. exam·grade·unit·file·date는 적지 않는다 — 폴더 이름에서 만든다
figures.py: 그 단원 그림만. 그림이 없으면 두지 않는다
figures/: 빌드가 만든 SVG(그림 p*.svg, 수식 m*·c*·a*·s*.svg)
수리소_시험대비_….html, .pdf: 빌드 결과. PDF 안의 만든 날짜는 학기 첫날(1학기 3. 1., 2학기 9. 1.)로 고정해 같은 입력이면 PDF도 바이트 단위로 같다


## 작업 순서
1. build/시험대비/<시험>/<학년>/<단원>/problems.md를 읽는다. templates/sample/의 problems.yaml과 figures.py를 그 옆에 복사한다
2. problems.yaml에 지문을 표기 규칙대로 옮기고 choices·answer·solution·figure·alt를 적는다. exam·grade·unit·file·date 줄은 지운다
3. figures.py에 그 단원 그림만 남긴다
4. `python .claude/skills/surisomath-exam/templates/build.py build/시험대비/<시험>/<학년>/<단원>/problems.yaml`
5. 경고가 없으면(종료 코드 0) 규격은 끝이다. `!` 줄은 무엇을 어떻게 고치라고 말해 준다. 그대로 고치고 다시 돌린다
6. PDF를 눈으로 본다. 각도 글이 선에 걸리는지, 그림이 문제와 맞는지, 선지 열이 관례대로인지는 눈으로만 안다
그림은 그대로 두고 글만 고칠 때는 --no-figures를 붙인다


## 쓰는 법
problems.yaml: problems[]만 있으면 된다. text · answer, 객관식이면 choices(다섯), 그림이 있으면 figure · alt, 선생님 풀이가 있으면 solution(`|` 블록)
text에 ": "가 들어가면 따옴표로 감싼다. 수식의 역슬래시는 따옴표 없는 글에서 그대로 살아 있다
번호를 직접 주려면 `"no": "004"` — 키까지 따옴표로
figures.py 뼈대: `import grind_figure as g` → `g.setup(__file__)` → 그림 함수들 → `if __name__ == "__main__":`에서 부른다. 연마와 같다
PDF 보기: `python -c "import fitz; d=fitz.open('…pdf'); [p.get_pixmap(dpi=110).save(f'{p.number}.png') for p in d]"`


## 참고
pip install pyyaml weasyprint pdfplumber matplotlib pymupdf
Windows는 GTK 런타임이 따로 필요하다: winget install tschoonj.GTKForWindows
```

- [ ] **Step 2: README에 절을 더한다**

`### surisomath-grind` 절의 빌드 명령 코드 블록 뒤(`## 서체` 앞)에 넣는다.

```markdown
### surisomath-exam

실전 모의 학습지 '시험대비' 양식. surisomath-a4 위에 얹고 연마의 그림 도우미와 수식 조판을
빌려 쓴다. 한 쪽에 문제 둘(두 단), 문제 아래는 학생이 손으로 푸는 빈 자리. 선생님 쪽에는
정답 줄과 풀이가 같은 자리에 붙는다. 낱말로 쓴 도형 표기(선분 AB, 각 A, 삼각형 ABC)를
교과서 기호로 조판한다.

```
.claude/skills/surisomath-exam/
├─ SKILL.md
└─ templates/
   ├─ exam.css      base.css 위에 얹는 시험대비 스타일(두 단·세로선·머리줄·이름 칸·선지 열)
   ├─ build.py      problems.yaml → 그림 → HTML → PDF → 검사. 연마 build.py의 rich()를 모듈로 쓴다
   └─ sample/       견본 두 문제. 새 단원은 이 폴더를 복사해 출발한다
```

```
python .claude/skills/surisomath-exam/templates/build.py build/시험대비/20262학기중간/중3/삼각비/problems.yaml
```

단원 폴더는 `build/시험대비/<시험>/<학년>/<단원>/`. 시험 폴더 이름(`20262학기중간`)에서 머리줄과
파일 이름을 만든다.
```

- [ ] **Step 3: 커밋**

```bash
git add .claude/skills/surisomath-exam/SKILL.md README.md
git commit -m "surisomath-exam: SKILL.md와 README"
```

---

### Task 7: 삼각비 problems.yaml

**Files:**
- Create: `build/시험대비/20262학기중간/중3/삼각비/problems.yaml`
- Track: `build/시험대비/20262학기중간/중3/삼각비/problems.md` (그대로, git add만)

정답은 이렇게 풀었다. 001 ④(`AH = CH tan55°`, `BH = CH tan38°`, `AH − BH = 80`). 002 `2/(tan50° − tan28°)`m. 003 `40√3/9`(넓이: `½·10·8·sin60° = ½·10·x·sin30° + ½·8·x·sin30°`). 004 `3/5`(△AMN = 16 − 4 − 4 − 2 = 6 = ½·(2√5)²·sin x). 005 182.35m(`124 = CH(1.96 − 1.28)`). 006 `24√3/7`cm. 007 2% 감소(0.7 × 1.4 = 0.98). 008 `8√3π/3`(AB가 축 위, BC = 2, 높이 2√3인 원뿔). 009 `11√10/25`(BE = 5, BF = 2√10, △EBF = 24 − 6 − 6 − 3 = 9 → sin x° = 9√10/50, cos x° = 13√10/50). 010 ①(0° < x < 45°에서 sin x < cos x라 둘 다 cos x − sin x).

- [ ] **Step 1: problems.yaml을 쓴다**

```yaml
# 시험대비 · 2026학년도 2학기 중간고사 · 중3 · 삼각비 — problems.md 열 문제. 지문은 problems.md
# 문장 그대로다. 표기만 조판했다(SKILL.md 표기 규칙): 선분 AB → \overline{\mathrm{AB}},
# 각 → \angle, 삼각형 → \triangle\!, 사각형 → □, 각도 → ^{\circ}, 루트 → \sqrt.
# 머리줄·파일 이름·PDF 날짜는 build.py가 폴더 이름에서 만든다.

problems:
  - text: 다음 그림과 같이 80m 떨어진 두 지점 $\mathrm{A}$, $\mathrm{B}$에서 산꼭대기를 올려다 본 각의 크기가 각각 $35^{\circ}$, $52^{\circ}$였을 때, 산의 높이 $\overline{\mathrm{CH}}$를 구하는 식은? (단, 눈의 높이는 무시한다.)
    figure: p1.svg
    alt: 각 CAH가 35°, 각 CBH가 52°, 각 CHA가 직각인 그림
    choices:
      - $\dfrac{80}{\tan 52^{\circ}+\tan 35^{\circ}}$
      - $\dfrac{80}{\tan 52^{\circ}-\tan 35^{\circ}}$
      - $\dfrac{80}{\tan 52^{\circ}+\tan 38^{\circ}}$
      - $\dfrac{80}{\tan 55^{\circ}-\tan 38^{\circ}}$
      - $\dfrac{80}{\tan 55^{\circ}-\tan 35^{\circ}}$
    answer: ④
    solution: |
      주어진 그림에서
      $\overline{\mathrm{AH}}=\overline{\mathrm{CH}}\tan 55^{\circ}$,
      $\overline{\mathrm{BH}}=\overline{\mathrm{CH}}\tan 38^{\circ}$이므로
      $\overline{\mathrm{AB}}=\overline{\mathrm{AH}}-\overline{\mathrm{BH}}$에서
      $80=\overline{\mathrm{CH}}(\tan 55^{\circ}-\tan 38^{\circ})$이다.
      따라서 $\overline{\mathrm{CH}}=\dfrac{80}{\tan 55^{\circ}-\tan 38^{\circ}}$이다.

  - text: 다음 그림과 같이 2m 떨어진 두 지점 $\mathrm{B}$, $\mathrm{C}$에서 기구를 올려다 본 각의 크기가 $40^{\circ}$, $62^{\circ}$일 때, 지상부터 기구가 있는 곳까지의 높이를 삼각비로 나타내시오.
    figure: p2.svg
    alt: 각 ABH가 40°, 각 ACH가 62°, 각 AHC가 직각인 그림
    answer: $\dfrac{2}{\tan 50^{\circ}-\tan 28^{\circ}}$m
    solution: |
      주어진 그림에서
      $\overline{\mathrm{BH}}=\overline{\mathrm{AH}}\tan 50^{\circ}$,
      $\overline{\mathrm{CH}}=\overline{\mathrm{AH}}\tan 28^{\circ}$이므로
      $\overline{\mathrm{BC}}=\overline{\mathrm{BH}}-\overline{\mathrm{CH}}$에서
      $2=\overline{\mathrm{AH}}(\tan 50^{\circ}-\tan 28^{\circ})$이다.
      따라서 지상부터 기구가 있는 곳까지의
      높이는 $\dfrac{2}{\tan 50^{\circ}-\tan 28^{\circ}}$m이다.

  - text: $\triangle\!\mathrm{ABC}$에서 $\angle\mathrm{A}=60^{\circ}$이고 $\overline{\mathrm{AD}}$는 $\angle\mathrm{A}$의 이등분선일 때, $\overline{\mathrm{AD}}$의 길이를 구하시오.
    figure: p3.svg
    alt: 선분 AB의 길이는 10, 선분 AC의 길이는 8
    answer: $\dfrac{40\sqrt{3}}{9}$
    solution: |
      $\overline{\mathrm{AD}}=x$라 하자.
      $\triangle\!\mathrm{ABC}=\triangle\!\mathrm{ABD}+\triangle\!\mathrm{ACD}$이고
      $\angle\mathrm{BAD}=\angle\mathrm{CAD}=30^{\circ}$이므로
      세 삼각형의 넓이를 삼각비로 나타내면
      $20\sqrt{3}=\dfrac{9}{2}x$이다.
      따라서 $\overline{\mathrm{AD}}=\dfrac{40\sqrt{3}}{9}$이다.

  - text: $□\mathrm{ABCD}$는 한 변의 길이가 4cm인 정사각형이다. $\overline{\mathrm{BC}}$, $\overline{\mathrm{CD}}$의 중점을 각각 $\mathrm{M}$, $\mathrm{N}$이라 하고 $\angle\mathrm{MAN}=x$라 할 때, $\sin x$의 값을 구하시오.
    figure: p4.svg
    alt: 문제의 조건대로 그려진 정사각형. 문제의 조건이 모두 표기되어 있음
    answer: $\dfrac{3}{5}$
    solution: |
      주어진 그림에서
      $\overline{\mathrm{AM}}=\overline{\mathrm{AN}}=2\sqrt{5}$(cm)이고
      $\triangle\!\mathrm{AMN}$의 넓이는 정사각형에서
      세 직각삼각형을 뺀 6cm²이다.
      한편
      $\triangle\!\mathrm{AMN}=\dfrac{1}{2}\times\overline{\mathrm{AM}}\times\overline{\mathrm{AN}}\times\sin x$
      이므로 $10\sin x=6$이다.
      따라서 $\sin x=\dfrac{3}{5}$이다.

  - text: 다음 그림과 같이 바다 위의 한 지점 $\mathrm{A}$에서 성산일출봉의 꼭대기 지점 $\mathrm{C}$를 올려다 본 각의 크기가 $27^{\circ}$이고, 지점 $\mathrm{A}$에서 산 쪽으로 124m 나아간 지점 $\mathrm{B}$에서 다시 지점 $\mathrm{C}$를 올려다 본 각의 크기가 $38^{\circ}$라고 한다. 성산일출봉의 높이는 해발 몇 m인지 구하시오. (단, $\tan 63^{\circ}=1.96$, $\tan 52^{\circ}=1.28$로 계산하고, 계산 결과는 소수점 아래 셋째 자리에서 반올림한다.)
    figure: p5.svg
    alt: 각 CAH가 27°, 각 CBH가 38°, 각 CHB가 직각인 그림
    answer: 182.35m
    solution: |
      주어진 그림에서
      $\overline{\mathrm{AH}}=\overline{\mathrm{CH}}\tan 63^{\circ}$,
      $\overline{\mathrm{BH}}=\overline{\mathrm{CH}}\tan 52^{\circ}$이므로
      $\overline{\mathrm{AB}}=\overline{\mathrm{AH}}-\overline{\mathrm{BH}}$에서
      $124=\overline{\mathrm{CH}}(\tan 63^{\circ}-\tan 52^{\circ})$이다.
      따라서 성산일출봉의 높이는
      해발 $\dfrac{124}{0.68}=182.35$(m)이다.

  - text: $\overline{\mathrm{AB}}=8$cm, $\overline{\mathrm{AC}}=6$cm, $\angle\mathrm{BAC}=60^{\circ}$인 $\triangle\!\mathrm{ABC}$에서 $\angle\mathrm{BAC}$의 이등분선이 $\overline{\mathrm{BC}}$와 만나는 점을 $\mathrm{D}$라 할 때, $\overline{\mathrm{AD}}$의 길이를 구하시오.
    figure: p6.svg
    alt: 선분 AB의 길이는 8cm, 선분 AC의 길이는 6cm
    answer: $\dfrac{24\sqrt{3}}{7}$cm
    solution: |
      $\overline{\mathrm{AD}}=x$라 하자.
      $\triangle\!\mathrm{ABC}=\triangle\!\mathrm{ABD}+\triangle\!\mathrm{ACD}$이고
      $\angle\mathrm{BAD}=\angle\mathrm{CAD}=30^{\circ}$이므로
      세 삼각형의 넓이를 삼각비로 나타내면
      $12\sqrt{3}=\dfrac{7}{2}x$이다.
      따라서 $\overline{\mathrm{AD}}=\dfrac{24\sqrt{3}}{7}$cm이다.

  - text: 다음 그림과 같이 직각삼각형 $\mathrm{ABC}$ $\overline{\mathrm{AB}}$의 길이를 30% 줄이고 $\overline{\mathrm{BC}}$의 길이는 40% 늘여서 새로운 $\triangle\!\mathrm{A}'\mathrm{BC}'$으로 만들었다. $\triangle\!\mathrm{A}'\mathrm{BC}'$의 넓이는 $\triangle\!\mathrm{ABC}$의 넓이에 비해 얼마나 변하였는지 구하시오.
    figure: p7.svg
    alt: 직각삼각형 ABC는 연하게 색칠되어 있음. 새로운 삼각형 A'BC'은 점선으로 그려져 있음
    answer: 2% 감소
    solution: |
      $\angle\mathrm{B}$는 두 삼각형에 공통이고
      $\overline{\mathrm{A}'\mathrm{B}}=0.7\,\overline{\mathrm{AB}}$,
      $\overline{\mathrm{BC}'}=1.4\,\overline{\mathrm{BC}}$이므로
      $\triangle\!\mathrm{A}'\mathrm{BC}'=\dfrac{1}{2}\times\overline{\mathrm{A}'\mathrm{B}}\times\overline{\mathrm{BC}'}\times\sin\mathrm{B}$
      $=0.7\times1.4\times\triangle\!\mathrm{ABC}$
      $=0.98\triangle\!\mathrm{ABC}$이다.
      따라서 $\triangle\!\mathrm{A}'\mathrm{BC}'$의 넓이는
      $\triangle\!\mathrm{ABC}$의 넓이보다 2% 줄어든다.

  - text: 다음 그림과 같이 직각삼각형 $\mathrm{ABC}$은 직선 $l$을 축으로 1회전한다. 이때 생기는 입체도형의 부피를 구하시오.
    figure: p8.svg
    alt: 직선 l은 수직. 점 A는 직선 l 위에 있고 선분 AC이 직선 l과 이루는 각도 30°. 선분 AC는 오른쪽 아래로 향하고 있음. 각 ACB는 60° 각 ABC는 직각. 선분 AB는 2루트3.
    answer: $\dfrac{8\sqrt{3}}{3}\pi$
    solution: |
      주어진 그림에서
      $\overline{\mathrm{BC}}=\overline{\mathrm{AB}}\tan 30^{\circ}=2$이다.
      회전체는 밑면의 반지름의 길이가 2,
      높이가 $2\sqrt{3}$인 원뿔이다.
      따라서 구하는 입체도형의 부피는
      $\dfrac{1}{3}\times\pi\times2^2\times2\sqrt{3}=\dfrac{8\sqrt{3}}{3}\pi$이다.

  - text: 다음 그림과 같이 $□\mathrm{ABCD}$는 가로와 세로의 길이가 각각 6, 4인 직사각형이고 두 점 $\mathrm{E}$, $\mathrm{F}$는 각각 $\overline{\mathrm{AD}}$, $\overline{\mathrm{CD}}$의 중점이다. $\sin x^{\circ}+\cos x^{\circ}$의 값을 구하시오.
    figure: p9.svg
    alt: 지문 조건대로 그림 그려져 있고, 각 EBF가 x°. 그리고 선분 EB, 선분 BF, 선분 EF가 그려져 있음. 그리고 중점 표시로 길이가 같다는 표시가 있음.
    answer: $\dfrac{11\sqrt{10}}{25}$
    solution: |
      주어진 그림에서
      $\overline{\mathrm{BE}}=5$, $\overline{\mathrm{BF}}=2\sqrt{10}$이고
      $\triangle\!\mathrm{EBF}$의 넓이는 직사각형에서
      세 직각삼각형을 뺀 9이다.
      한편
      $\triangle\!\mathrm{EBF}=\dfrac{1}{2}\times\overline{\mathrm{BE}}\times\overline{\mathrm{BF}}\times\sin x^{\circ}$
      이므로 $\sin x^{\circ}=\dfrac{9\sqrt{10}}{50}$이다.
      $\sin^2 x^{\circ}+\cos^2 x^{\circ}=1$이고
      $0^{\circ}<x^{\circ}<90^{\circ}$이므로
      $\cos x^{\circ}=\dfrac{13\sqrt{10}}{50}$이다.
      따라서 $\sin x^{\circ}+\cos x^{\circ}=\dfrac{11\sqrt{10}}{25}$이다.

  - text: $0^{\circ}<x<45^{\circ}$일 때, $\sqrt{(\sin x-\cos x)^2}-\sqrt{(\cos x-\sin x)^2}$을 간단히 하면?
    choices:
      - $0$
      - $2\sin x$
      - $2\cos x$
      - $2\sin x-2\cos x$
      - $2\cos x-2\sin x$
    answer: ①
    solution: |
      $0^{\circ}<x<45^{\circ}$에서 $\sin x<\cos x$이므로
      $\sin x-\cos x<0$, $\cos x-\sin x>0$이다.
      그러므로
      $\sqrt{(\sin x-\cos x)^2}=\cos x-\sin x$,
      $\sqrt{(\cos x-\sin x)^2}=\cos x-\sin x$이다.
      따라서 주어진 식을 간단히 하면 $0$이다.
```

- [ ] **Step 2: yaml이 읽히는지, 지문이 원문과 표기 말고 같은지 본다**

```bash
python -c "import yaml,sys; d=yaml.safe_load(open(sys.argv[1],encoding='utf-8')); print(len(d['problems']), [('choices' in p, 'figure' in p) for p in d['problems']])" "build/시험대비/20262학기중간/중3/삼각비/problems.yaml"
```
Expected: `10 [(True, True), (False, True), (False, True), (False, True), (False, True), (False, True), (False, True), (False, True), (False, True), (True, False)]`.

problems.md와 yaml을 나란히 놓고 열 문제의 지문을 읽어 표기 말고 다른 글자가 없는지 맞춘다. 특히 007의 "직각삼각형 ABC 선분 AB의"(조사가 없는 원문 그대로), 005의 괄호 조건, 010의 "을 간단히 하면?".

- [ ] **Step 3: 커밋(원문 md도 함께)**

```bash
git add "build/시험대비/20262학기중간/중3/삼각비/problems.md" "build/시험대비/20262학기중간/중3/삼각비/problems.yaml"
git commit -m "시험대비 삼각비: 원문 열 문제와 조판본·정답·풀이"
```

---

### Task 8: 삼각비 figures.py

**Files:**
- Create: `build/시험대비/20262학기중간/중3/삼각비/figures.py`

점 이름·길이 글은 `g.name`, 각은 `g.angle`. 좌표는 문제의 단위(m, cm)를 그대로 쓴다. y 범위는 시제품에서 save()가 일러 준 값이다(p1·p3·p7·p8·p9). 나머지(p2·p4·p5·p6)는 어림이라 첫 빌드의 `!` 줄이 주는 값으로 바꾼다.

- [ ] **Step 1: figures.py를 쓴다**

```python
# -*- coding: utf-8 -*-
"""시험대비 · 2026학년도 2학기 중간고사 · 중3 · 삼각비 — 문제 그림 아홉 장.

도우미는 surisomath-grind 스킬의 grind_figure를 쓴다. build.py가 import 경로를 잡아
주므로 이 파일은 build.py로 실행한다.

    python .claude/skills/surisomath-exam/templates/build.py build/시험대비/20262학기중간/중3/삼각비/problems.yaml

그림은 단 글 너비(229pt) 안에 들어야 한다. y 범위가 배율을 정하므로 넓은 그림(001·005의
산)은 y 범위를 넉넉히 잡아 배율을 낮춘다. save()가 위아래 여백을 재서 y 범위를 일러 준다.
삽화는 없다. 산·기구·성산일출봉은 지면 선 위의 삼각형이다. 길이는 교과서처럼 변 옆에
글로 적고, 각은 호와 각도 글로 표시한다.
"""
from __future__ import annotations

import math

import grind_figure as g

g.setup(__file__)

TAN, RAD = math.tan, math.radians


def mid(p, q):
    return ((p[0] + q[0]) / 2, (p[1] + q[1]) / 2)


# ── 두 지점에서 올려다본 꼭대기(001 · 002 · 005) ────────────────────────
# 먼 점 far, 가까운 점 near가 지면 위에 있고 꼭대기 top의 발 H가 그 오른쪽에 있다.
# 지면 선 far–H, 세 변, H의 직각, 두 각(호 + 각도 글), 두 지점 사이 길이 글.

def elevation(ax, far, near, top, a_far, a_near, base_text, names):
    H = (top[0], far[1])
    g.seg(ax, far, H)
    g.seg(ax, far, top)
    g.seg(ax, near, top)
    g.seg(ax, top, H)
    g.right_angle(ax, H, -1, 1)
    g.angle(ax, far, H, top, "$%d^{\\circ}$" % a_far, r=16)
    g.angle(ax, near, H, top, "$%d^{\\circ}$" % a_near, r=11)
    g.name(ax, mid(far, near), base_text, dy=-5, va="top")
    g.name(ax, far, names[0], dy=-5, va="top")
    g.name(ax, near, names[1], dy=-5, va="top")
    g.name(ax, top, names[2], dy=5, va="bottom")
    g.name(ax, H, names[3], dy=-5, va="top")


def p1():
    """A·B가 80m, 각 35°·52°. CH = 80 / (1/tan35° − 1/tan52°) ≈ 123.6m."""
    h = 80 / (1 / TAN(RAD(35)) - 1 / TAN(RAD(52)))
    A, B = (0.0, 0.0), (80.0, 0.0)
    C = (h / TAN(RAD(35)), h)
    f, ax = g.canvas(5, -31.9, 155.6)
    elevation(ax, A, B, C, 35, 52, "80m", "ABCH")
    g.save(f, "p1.svg")


def p2():
    """B·C가 2m, 각 40°·62°, 기구 A. AH = 2 / (1/tan40° − 1/tan62°) ≈ 3.03m."""
    h = 2 / (1 / TAN(RAD(40)) - 1 / TAN(RAD(62)))
    B, C = (0.0, 0.0), (2.0, 0.0)
    A = (h / TAN(RAD(40)), h)
    f, ax = g.canvas(5, -0.9, 3.6)
    elevation(ax, B, C, A, 40, 62, "2m", "BCAH")
    g.save(f, "p2.svg")


def p5():
    """A·B가 124m, 각 27°·38°. CH ≈ 182.4m. 밑변이 길어 배율을 더 낮춘다."""
    h = 124 / (1 / TAN(RAD(27)) - 1 / TAN(RAD(38)))
    A, B = (0.0, 0.0), (124.0, 0.0)
    C = (h / TAN(RAD(27)), h)
    f, ax = g.canvas(5, -70.0, 215.0)
    elevation(ax, A, B, C, 27, 38, "124m", "ABCH")
    g.save(f, "p5.svg")


# ── 각의 이등분선(003 · 006) ─────────────────────────────────────────────
# A가 위, ∠A = 60°. AB는 240° 방향, AC는 300° 방향이라 이등분선 AD는 곧게 아래로 내려간다.
# BD : DC = AB : AC. A의 두 각에 같은 각 표시(호 + 획).

def bisector(ax, ab, ac, tb, tc):
    A = (0.0, 0.0)
    B = g.polar(A, ab, 240)
    C = g.polar(A, ac, 300)
    t = ab / (ab + ac)
    D = (B[0] + (C[0] - B[0]) * t, B[1] + (C[1] - B[1]) * t)
    g.poly(ax, [A, B, C])
    g.seg(ax, A, D)
    g.angle(ax, A, B, D, "", r=12, ticks=1)
    g.angle(ax, A, D, C, "", r=12, ticks=1)
    g.name(ax, A, "A", dy=5, va="bottom")
    g.name(ax, B, "B", dx=-4, dy=-4, ha="right", va="top")
    g.name(ax, C, "C", dx=4, dy=-4, ha="left", va="top")
    g.name(ax, D, "D", dy=-5, va="top")
    g.name(ax, mid(A, B), tb, dx=-6, dy=3, ha="right", va="bottom")
    g.name(ax, mid(A, C), tc, dx=6, dy=3, ha="left", va="bottom")


def p3():
    f, ax = g.canvas(5, -10.76, 2.19)
    bisector(ax, 10.0, 8.0, "10", "8")
    g.save(f, "p3.svg")


def p6():
    f, ax = g.canvas(5, -8.7, 1.8)
    bisector(ax, 8.0, 6.0, "8cm", "6cm")
    g.save(f, "p6.svg")


# ── 004 — 정사각형의 두 중점 ─────────────────────────────────────────────
# A 왼쪽 위, B 왼쪽 아래, C 오른쪽 아래, D 오른쪽 위. M은 BC(아래 변), N은 CD(오른 변)의 중점.
# 같은 길이 표시: BM·MC 한 획, CN·ND 두 획. 4cm는 왼 변 옆에.

def p4():
    s = 4.0
    A, B, C, D = (0.0, s), (0.0, 0.0), (s, 0.0), (s, s)
    M, N = (s / 2, 0.0), (s, s / 2)
    f, ax = g.canvas(6, -0.9, 4.9)
    g.rect(ax, B, s, s)
    g.seg(ax, A, M)
    g.seg(ax, A, N)
    g.seg(ax, M, N)
    g.angle(ax, A, M, N, "$x$", r=14)
    g.tick(ax, B, M)
    g.tick(ax, M, C)
    g.tick(ax, C, N, n=2)
    g.tick(ax, N, D, n=2)
    g.name(ax, A, "A", dx=-4, dy=4, ha="right", va="bottom")
    g.name(ax, B, "B", dx=-4, dy=-4, ha="right", va="top")
    g.name(ax, C, "C", dx=4, dy=-4, ha="left", va="top")
    g.name(ax, D, "D", dx=4, dy=4, ha="left", va="bottom")
    g.name(ax, M, "M", dy=-5, va="top")
    g.name(ax, N, "N", dx=5, ha="left")
    g.name(ax, mid(A, B), "4cm", dx=-5, ha="right")
    g.save(f, "p4.svg")


# ── 007 — 변을 줄이고 늘인 직각삼각형 ────────────────────────────────────
# B가 직각. A'는 BA 위(0.7배), C'는 BC의 연장 위(1.4배). ABC는 10% 틴트, A'BC'은 점선.

def p7():
    A, B, C = (0.0, 4.0), (0.0, 0.0), (5.0, 0.0)
    A2, C2 = (0.0, 4.0 * 0.7), (5.0 * 1.4, 0.0)
    f, ax = g.canvas(5, -1.04, 4.46)
    g.shade(ax, [A, B, C])
    g.poly(ax, [A, B, C])
    g.poly(ax, [A2, B, C2], dashed=True)
    g.right_angle(ax, B, 1, 1)
    g.name(ax, A, "A", dx=-4, ha="right")
    g.name(ax, B, "B", dx=-4, dy=-4, ha="right", va="top")
    g.name(ax, C, "C", dy=-5, va="top")
    g.name(ax, A2, "$\\mathrm{A}'$", dx=-4, ha="right")
    g.name(ax, C2, "$\\mathrm{C}'$", dy=-5, va="top")
    g.save(f, "p7.svg")


# ── 008 — 회전축 위의 직각삼각형 ─────────────────────────────────────────
# ∠A = 30°라 AB가 축 l 위에 있다. A가 위, B가 아래, C가 오른쪽. 축은 A 위와 B 아래로 더 뻗는다.

def p8():
    ab = 2 * math.sqrt(3)
    A, B = (0.0, ab), (0.0, 0.0)
    C = (ab * TAN(RAD(30)), 0.0)
    f, ax = g.canvas(7, -1.21, 5.06)
    g.axis(ax, (0.0, -1.0), (0.0, ab + 1.2), "$l$")
    g.poly(ax, [A, B, C])
    g.right_angle(ax, B, 1, 1)
    g.angle(ax, A, B, C, "$30^{\\circ}$", r=16)
    g.angle(ax, C, A, B, "$60^{\\circ}$", r=11)
    g.name(ax, A, "A", dx=-5, ha="right")
    g.name(ax, B, "B", dx=-5, dy=-3, ha="right", va="top")
    g.name(ax, C, "C", dx=5, dy=-3, ha="left", va="top")
    g.name(ax, (0.0, ab / 2), "$2\\sqrt{3}$", dx=-6, ha="right")
    g.save(f, "p8.svg")


# ── 009 — 직사각형의 두 중점 ─────────────────────────────────────────────
# A 왼쪽 위, B 왼쪽 아래, C 오른쪽 아래, D 오른쪽 위. E는 AD(위 변), F는 CD(오른 변)의 중점.
# 각 x°는 B에서 BF와 BE 사이. 같은 길이 표시: AE·ED 한 획, CF·FD 두 획.

def p9():
    w, h = 6.0, 4.0
    A, B, C, D = (0.0, h), (0.0, 0.0), (w, 0.0), (w, h)
    E, F = (w / 2, h), (w, h / 2)
    f, ax = g.canvas(5, -1.03, 5.03)
    g.rect(ax, B, w, h)
    g.seg(ax, E, B)
    g.seg(ax, B, F)
    g.seg(ax, E, F)
    g.angle(ax, B, F, E, "$x^{\\circ}$", r=14)
    g.tick(ax, A, E)
    g.tick(ax, E, D)
    g.tick(ax, C, F, n=2)
    g.tick(ax, F, D, n=2)
    g.name(ax, A, "A", dx=-4, dy=4, ha="right", va="bottom")
    g.name(ax, B, "B", dx=-4, dy=-4, ha="right", va="top")
    g.name(ax, C, "C", dx=4, dy=-4, ha="left", va="top")
    g.name(ax, D, "D", dx=4, dy=4, ha="left", va="bottom")
    g.name(ax, E, "E", dy=5, va="bottom")
    g.name(ax, F, "F", dx=5, ha="left")
    g.name(ax, (w / 2, 0.0), "6", dy=-5, va="top")
    g.name(ax, (0.0, h / 2), "4", dx=-5, ha="right")
    g.save(f, "p9.svg")


if __name__ == "__main__":
    print("figures/")
    p1()
    p2()
    p3()
    p4()
    p5()
    p6()
    p7()
    p8()
    p9()
```

- [ ] **Step 2: 그림만 먼저 돌려 여백 경고를 잡는다**

```bash
cd "build/시험대비/20262학기중간/중3/삼각비" && PYTHONPATH="../../../../../.claude/skills/surisomath-grind/templates" python figures.py; cd -
```
Expected: 아홉 줄 `pN.svg  (W×Hpt)`. `!` 줄이 있으면 그 문장이 주는 y 범위를 `g.canvas(units, y0, y1)`에 넣고 다시 돌린다. 두어 번이면 다 사라진다. W는 전부 229 아래여야 한다(p5가 가장 넓다. 넘으면 y 범위를 위아래로 더 벌린다).

- [ ] **Step 3: 커밋**

```bash
git add "build/시험대비/20262학기중간/중3/삼각비/figures.py"
git commit -m "시험대비 삼각비: 그림 아홉"
```

---

### Task 9: 빌드 · 검사 · 눈으로 보기

**Files:**
- Build output: `build/시험대비/20262학기중간/중3/삼각비/figures/`, `수리소_시험대비_2026_2학기중간_중3_삼각비.html`, `.pdf`

- [ ] **Step 1: 빌드한다**

```bash
python .claude/skills/surisomath-exam/templates/build.py "build/시험대비/20262학기중간/중3/삼각비/problems.yaml"
```
Expected:
```
figures/
  p1.svg  (…×110pt)
  …
…\수리소_시험대비_2026_2학기중간_중3_삼각비.html
…\수리소_시험대비_2026_2학기중간_중3_삼각비.pdf  (10쪽)
  그리드 검사: N행 중 이상 없음
  풀 자리: 가장 좁은 단 001 10.5칸
```
종료 코드 0. 001이 10.5칸(±0.5. 지문이 다섯 줄로 떨어지면 9.5)이고 8칸 위다.

경고가 나면 이렇게 고친다.
- `문제 블록이 단 바닥을 넘는다` → 그 문제의 그림 units를 줄인다(y 범위를 넓히면 배율이 낮아진다)
- `잇단 줄의 수식이 겹친다` → 그 풀이에서 근호 든 분수가 위아래 줄에 겹친 것이다. 줄을 나누거나 앞뒤 글 길이를 바꿔 가로로 어긋나게 한다
- `풀이 글이 단 바닥을 넘는다` → 풀이 줄 수를 줄인다(가장 긴 009가 정답 줄 포함 12줄, 풀 자리 16.5칸)
- `정답 줄 … 넘는다` → 정답을 짧게

- [ ] **Step 2: 열 쪽을 눈으로 본다**

```bash
python -c "import fitz; d=fitz.open('build/시험대비/20262학기중간/중3/삼각비/수리소_시험대비_2026_2학기중간_중3_삼각비.pdf'); [p.get_pixmap(dpi=110).save(f'$TMP/tri{p.number}.png') for p in d]"
```
쪽마다 볼 것.
- 1쪽: 머리줄과 이름 칸. 001의 그림에서 35°·52° 글이 변에 안 걸리는지, 80m·A·B·H가 서로 안 겹치는지. 001 선지 다섯 줄이 분수로 서는지
- 2쪽: 003 그림의 같은 각 획 둘, 004의 정사각형 네 직각과 중점 획
- 3쪽: 005 그림이 단 안에 드는지(가장 넓다), 006
- 4쪽: 007의 색칠(연한 회색)과 점선 A'C'·CC', 008의 일점쇄선 축과 $2\sqrt{3}$
- 5쪽: 009의 x° 호, 010 선지 다섯 줄(1열)
- 6~10쪽: 풀 자리 첫 줄 회색 정답, 풀이가 단 안에서 끝나는지, 근호 든 분수가 위 줄과 안 겹치는지

글이 선에 걸리면 figures.py의 dx·dy·r을 바꿔 다시 빌드한다(`--no-figures` 없이).

- [ ] **Step 3: 다시 빌드해도 같은지 본다**

```bash
python .claude/skills/surisomath-exam/templates/build.py "build/시험대비/20262학기중간/중3/삼각비/problems.yaml" && git status --short "build/시험대비/"
```
Expected: 두 번째 빌드 뒤 git status에 첫 빌드 결과(아직 안 올린 figures/·html·pdf)만 `??`로 있고, 이미 올린 파일은 안 바뀐다.

- [ ] **Step 4: 커밋**

```bash
git add "build/시험대비/20262학기중간/중3/삼각비/"
git commit -m "시험대비 삼각비: 학생 쪽 다섯 장과 선생님 쪽 다섯 장"
```

---

## 자기 검토

- 설계 문서의 절과 작업: 쪽의 짜임·단·이름 칸 → Task 3, 문제 짜임·선지 열·표기 규칙 → Task 4·7, 선생님 쪽 → Task 4·7, 그림 도우미 → Task 2, 그림 목록 → Task 8, 파일·빌드·검사 아홉 항목 → Task 4(`check`·`report`·`figure_html`·`check_overlap`·render --check), 견본 → Task 5, SKILL·README → Task 6, 확인 기준 → Task 9
- 이름 맞춤: `g.angle(ax, v, p, q, text, r, ticks)`·`g.tick(ax, p, q, n)`·`g.axis(ax, p, q, text)`·`g.name(ax, p, s, dx, dy, ha, va)`·`g.poly(…, dashed=True)`는 Task 2 정의와 Task 5·8 호출이 같다. build.py의 `check`·`meta`·`report`·`choice_cols`는 Task 4 정의와 Task 4 Step 2 검사가 같다. HTML의 `div.work.box`는 render.py(Task 1)의 `box` 검사와 exam.css(Task 3)의 `.work`와 build.py(Task 4)의 `report`가 같은 이름을 본다
- 자리표시자 없음. 그림의 어림 y 범위(p2·p4·p5·p6)는 save()의 `!` 줄이 정확한 값을 주므로 첫 빌드에서 확정한다
